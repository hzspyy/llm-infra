# 2026-09-12

- 执行计划 E3 的 5.10 多 LoRA 服务：正文、mini、vLLM 1/4/7/8/9/16/64 adapter 扫描与 SGLang kernel 对拍材料已交付。固定 8 GPU 槽位、64 等长请求，8→9 时分两轮，64 时分八轮；原始数据保存在 `results/crater/lora/20260912-0010/`，派生统计在 `results/local/lora-scaling-analysis-20260912.json`。rank 超限后的恢复失败保留，不标整章验收完成。下一执行批次 5.11 API/SSE。
- 修复多行 SVG 被 Markdown 插入段落导致图形丢失、轴标签串成文字；build.py 对 SVG 整体保护。纯文本日志取消行号，GPU 占用/退出码带字段说明并折叠，代码行号独立显示。显式原始文件链接打包到 `site/raw/results/`，确保独立离线阅读。
- 构建 41 正文页、171 源码页，链接检查 214 页断链 0。5.10 桌面/iPad 深浅主题、MathML、SVG 子元素、折叠、源码行锚点与 10 个 raw 链接已检查。全书 coverage 仍有 5.5 的 custom_class 范围待决项。未提交或推送。
- 补写本地环境说明：`ENVIRONMENTS.md` 新增「本地环境」与「大额存储放外置盘」两节（macOS 26.3.1 arm64、构建 venv `/Volumes/data/venvs/llm-infra` Python 3.12.12、外置盘 `/dev/disk7s2` 931 GiB），`AGENTS.md` 的隔离条目改为「进仓库的产出留项目目录，大体积中间产物与临时工作区放 `/Volumes/data`，不用 `/tmp`」。起因：本机不关机、`/tmp` 不清理且占内置盘，之前把 vLLM/SGLang 源码快照放在了 `/tmp`。该快照已用 `rsync -a` 迁到 `/Volumes/data/artifacts/llm-infra/scratch/apisrc-vllm0.29.0-sglang0.5.19/`（421 文件）并删除 `/tmp` 副本。
- 执行计划 E3 的 5.11 API 与协议层：正文 `src/L5/5.11-api-protocol-layer.md`，新增 `labs/L5/api_layer_audit.py`、`sse_head_of_line.py`、`mini_sse.py`、`summarize_api_layer.py`、`run_api_layer_audit.sh`。原始工件 `results/crater/api/20260912-0325/`，退出码全 0，运行前后 GPU 空闲。
- 5.11 关键结论：24 token 流式响应在 HTTP/1.1 上是 chunked + SSE 两层分帧（25 帧 / 6211 字节 / 24 个 delta 事件）；层间四段分解闭合 0.79 + 0.005 + 76.32 + 0.63 = 77.75 ms，非推理部分 1.84%；tokenization 约 0.59 µs/token，合批比逐条省约 4 倍；事件循环对照中 in-loop 编码让别的请求多等 11.4 ms（等于自身编码耗时），`to_thread` 后尖峰消失但自身多等约 20 ms；keep-alive 注释是独立的 14 字节 chunked 帧。构建 42 正文页 / 185 源码页，链接检查 229 页断链 0，机制覆盖全通过（此前挂着的 5.5 `custom_class` 项已闭合）。并发 32 三次运行 p50 差幅超 20%，只保留趋势。下一执行批次 5.12。
- 源码引用做法：被正文引用的 vLLM/SGLang 安装源码快照进 `results/crater/<批次>/source/`（本批 14 个文件，544 KB），正文用项目相对路径引用；外置盘上的完整快照只作离线阅读，不参与构建。踩坑：`build.py` 的结果链接只接受**文件**，指向目录会抛 `ValueError: Invalid result link`，正文里不能链接 `source/` 目录本身。
- 5.11 验收：构建 42 正文页 / 185 源码页，链接检查 229 页断链 0，机制覆盖全通过；`outputs/api-layer-review-20260912/browser-checks.json` 在 1280×900 与 834×1112、深浅主题下 0 问题（无溢出、SVG 无插入段落、17 折叠块、20 高亮、无外部资源）；23 个 raw 文件与 14 个源码页经本地服务实测 200。未提交、未推送、未更新线上发布。下一执行批次 5.12 非生成模型的服务。
- `/tmp` 清理：把 `/tmp/llm-infra-*` 5 个旧临时文件（约 460 KiB）迁到 `/Volumes/data/artifacts/llm-infra/scratch/from-tmp-20260912/`，`/tmp` 已无本项目残留；归档路径登记在 `ENVIRONMENTS.md`。
