---
machine: crater (RTX 5090 D, sm_120) + worldvln (L40S, sm_89)
measured: 2026-09-10
deps: L0.2（roofline 与账本）
---

## 本章回答三个问题

1. 从寄存器到显存，每一级的**延迟**和**带宽**各是多少？拐点在哪里？这些数字规格表上没有。
2. 要把显存带宽打满，需要多少并发？为什么"线程开得越多越快"是错的？
3. FlashAttention 的 tile 尺寸为什么是 `<128, 64, 128, 4>` 而不是别的数？

先区分两个概念：延迟是一次访问需要等多久，带宽是单位时间能搬多少数据。
GPU 可以让许多访问同时进行，因此带宽高不代表单次访问延迟低。
本章先认识 SM（执行线程块的计算单元）及其寄存器、共享内存和缓存，
再用测量结果理解分块计算。硬件容量能排除放不下的 tile，
但不能单独决定哪组 tile 参数最快。

---

## 心智模型：一座延迟递增、带宽递减的金字塔

<figure>
<svg viewBox="0 0 740 400" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="GPU 内存层次金字塔">
  <g font-size="11.5" fill="currentColor">
    <text x="12" y="18" font-size="12.5" font-weight="600">RTX 5090 D（sm_120）· 全部为本机实测</text>

    <!-- registers -->
    <rect x="250" y="34" width="200" height="34" fill="none" stroke="currentColor" stroke-width="1.4"/>
    <text x="350" y="55" text-anchor="middle" font-size="12">寄存器</text>
    <text x="465" y="49" font-size="10.5" opacity="0.8">~1 周期 · 每 SM 256 KB</text>
    <text x="465" y="63" font-size="10" opacity="0.6">编译器分配，spill 就掉到 local memory</text>

    <!-- smem / L1 -->
    <rect x="215" y="78" width="270" height="44" fill="none" stroke="currentColor" stroke-width="1.4"/>
    <text x="350" y="97" text-anchor="middle" font-size="12">共享内存 / L1（每 SM 128 KiB 统一）</text>
    <text x="350" y="114" text-anchor="middle" font-size="10.5" opacity="0.85">smem 6.5 周期 · L1 命中 44 周期 (15.2 ns)</text>
    <text x="500" y="97" font-size="10.5" opacity="0.8">聚合带宽 27 TB/s</text>
    <text x="500" y="112" font-size="10" opacity="0.6">单 block 最多可申请 99 KB</text>

    <!-- L2 -->
    <rect x="165" y="132" width="370" height="52" fill="none" stroke="currentColor" stroke-width="1.4"/>
    <text x="350" y="152" text-anchor="middle" font-size="12">L2（全卡共享，96 MiB）</text>
    <text x="350" y="170" text-anchor="middle" font-size="10.5" opacity="0.85">360 周期 (124 ns) · 带宽 5.5 TB/s</text>
    <text x="548" y="158" font-size="10" opacity="0.65">Blackwell 的 L2 大到</text>
    <text x="548" y="172" font-size="10" opacity="0.65">能装下不少真实工作集</text>

    <!-- DRAM -->
    <rect x="100" y="194" width="500" height="58" fill="none" stroke="currentColor" stroke-width="1.4"/>
    <text x="350" y="215" text-anchor="middle" font-size="12">显存 GDDR7（32 GB）</text>
    <text x="350" y="233" text-anchor="middle" font-size="10.5" opacity="0.85">929 周期 (320 ns) · 带宽 1674 GB/s</text>
    <text x="350" y="248" text-anchor="middle" font-size="10" opacity="0.7">decode 每步要从这里搬 3.44 GB 权重</text>

    <!-- host -->
    <rect x="60" y="262" width="580" height="40" fill="none" stroke="currentColor" stroke-width="1" stroke-dasharray="4 3"/>
    <text x="350" y="287" text-anchor="middle" font-size="11.5" opacity="0.8">主机内存（经 PCIe 5.0，实测 47.5 GB/s）—— 比显存慢 35 倍，见 L1.3</text>

    <!-- key insight -->
    <line x1="12" y1="320" x2="728" y2="320" stroke="currentColor" stroke-width="1" opacity="0.3"/>
    <text x="12" y="344" font-size="12" font-weight="600">带宽-延迟积：要打满 1674 GB/s，必须始终有 <tspan font-size="13">524 KB</tspan> 的访存在途</text>
    <text x="12" y="364" font-size="11" opacity="0.85">= 1674 GB/s × 320 ns。这就是「为什么要开很多 warp」的定量答案。</text>
    <text x="12" y="386" font-size="11" opacity="0.85">L40S 只需要 169 KB（681 GB/s × 253 ns）—— <tspan font-weight="600">带宽越高的卡，越难喂饱</tspan>。</text>
  </g>
</svg>
<figcaption>每一级的数字都来自 <code>labs/L1/mem_hierarchy.cu</code> 的实测，不是厂商标称。</figcaption>
</figure>

---

## 机制拆解

### SIMT：warp 是真正的调度单位

GPU 的最小调度单位不是线程，是 **warp = 32 个线程**。一条指令被整个 warp 同时执行
（Single Instruction, Multiple Threads）。三个直接后果：

1. **分支发散（divergence）**：warp 内如果 `if` 两侧都有线程走，硬件会**两条路径都执行一遍**，
   用掩码屏蔽不该生效的线程。实际代价取决于分支长度、活跃线程和编译结果，
   不能只看到 `if (tid % 2)` 就认定性能减半。
2. **访存合并（coalescing）**：warp 的 32 个线程如果访问连续地址，硬件合并成少数几个
   128 字节事务；如果地址散开，最坏要发 32 个事务。这是 L2.3 的主题。
3. **延迟靠切换掩盖**：一个 warp 卡在等访存时，SM 立刻切到另一个就绪的 warp。
   **GPU 不像 CPU 那样靠乱序执行和大缓存掩盖延迟，它靠"手上有足够多的活"。**

本机实测：每 SM 最多常驻 **1536 个线程 = 48 个 warp**，共 **170 个 SM**。

### 延迟：三段平台，拐点精确落在容量边界

指针追逐（每次访存依赖上一次结果，硬件无法并行发射）测出的**纯延迟**：

| 工作集 | 5090 周期 | 5090 ns | L40S 周期 | L40S ns | 命中层级 |
|---|---|---|---|---|---|
| 4–64 KiB | **44.1** | 15.22 | **40.0** | 15.91 | L1 |
| 128 KiB | 285.0 | 98.24 | 222.1 | 88.38 | L1 边界（部分被 smem 占走） |
| 256 KiB – 64 MiB | **360** | 124.1 | **279** | 111.0 | L2 |
| 96 MiB | 575.6 | 198.4 | 354.2 | 141.0 | L2 已到容量边界 |
| 128–512 MiB | **929** | 320.3 | **636** | 253.4 | 显存 |

三段平台干净到令人愉快：**44 / 360 / 929**（5090），**40 / 279 / 636**（L40S）。
中间的两个跳变分别发生在 128 KiB 和 96 MiB —— 正好是 L1 与 L2 的容量。

**跨架构的观察很反直觉**：5090 在**每一级**的延迟都比 L40S 高
（L2 360 vs 279 周期，显存 929 vs 636 周期），但它的带宽高 2.5 倍。

这是 GDDR7 的典型形态：靠更多的 bank 与更深的流水换带宽，代价是单次访问的往返更长。
**「更快的显存」在延迟意义上可能更慢。** 对 decode 这种高并发访存的负载无所谓；
对访存链很长又并发不足的 kernel 则是实打实的损失。

### 带宽：随工作集掉三级

| 工作集 | 5090 GB/s | L40S GB/s | 主要命中 |
|---|---|---|---|
| 1 MiB | 10 427 | 9 511 | L1（各 SM 私有） |
| 4 MiB | 24 071 | 17 476 | L1 |
| **16 MiB** | **27 017** | **19 396** | L1（接近聚合上限） |
| 32 MiB | 12 866 | 9 362 | 开始溢出 L1 |
| 64–96 MiB | ~5 400 | ~4 270 | **L2** |
| 128 MiB | 3 006 | 1 644 | 过渡 |
| 512 MiB – 1 GiB | **1 674** | **681** | **显存** |

两个边界都能被解释：

- **第一个悬崖在 16 → 32 MiB**：全卡 L1 聚合容量 = 170 SM × 128 KiB = **21.25 MiB**。
  工作集超过它就掉进 L2。
- **第二个悬崖在 96 → 128 MiB**：正好是 L2 的 96 MiB。

**层级之间的带宽比：L1 : L2 : 显存 ≈ 16 : 3.3 : 1。**
这说明把可复用数据保留在片上值得考虑。tiling 是把大问题分成小块，
在一块数据被移出之前尽量重复使用它。带宽比不等于程序加速比，
因为搬入、同步、计算和写回也需要时间。L2.4 会逐步讨论这些成本。

::: note 这解释了 L0.2 里一个悬而未决的数字
L0.2 用 `probe_hw.py` 测出 copy 带宽 1519 GB/s、只读 1608 GB/s。
本章用完全不同的 kernel（float4 grid-stride 读）测出 1674 GB/s。
三个数字都在 1.5–1.7 TB/s 区间，互相印证。
而厂商标称是 1792 GB/s —— 实测能到 **93%**，已经相当接近上限。
:::

### 共享内存与 bank conflict

共享内存分成 **32 个 bank**，每 bank 4 字节宽。一个 warp 的 32 个线程如果落在
32 个不同 bank，一拍完成；如果全撞同一个 bank，就要串行。

实测（每线程连发 8 个互不依赖的 load）：

| 访问模式 | 5090 | L40S |
|---|---|---|
| stride=1（分散在 32 个 bank） | **6.51** 周期/次 | 6.65 |
| stride=32（全撞同一个 bank） | **65.79** 周期/次 | 65.55 |
| 冲突倍数 | **10.1×** | 9.9× |

::: warning 这个实验第一版是错的，值得记住为什么
第一版用了依赖链（`idx = s[idx]`），测出 stride=1 与 stride=32 都是 44 周期，
**冲突倍数 1.0×——什么都没测到**。

原因：bank conflict 损失的是**吞吐**，而依赖链一次只有一个在途请求，吞吐根本不是瓶颈。
必须让每个线程同时发出多个**互不依赖**的访存，冲突才会显现。

一般规律：**测延迟用依赖链，测吞吐用独立操作。** 用错了就会测出「没有效应」的假结论。
:::

::: note 这里的 10.1× 只是时间差，机制在 L2.6 被直接读了出来
本节测到的是**时间比**，「这是 bank conflict」是一个解释，不是观测。
时间变慢有很多可能的原因。

L2.6 在 spark 上用硬件计数器直接读了同一件事（那台机器能读性能计数器）：
同样的三元对照（stride 1 / 32 / 33），`wavefront / request` 分别是
**1.00 / 32.00 / 1.00**，冲突计数 `0 / 6,094,848 / 0`，
而 $6{,}094{,}848 = 196{,}608 \times 31$ 与请求数吻合。

**判据是 `wavefront/request` 这个比值，不是时间。** 它直接给出冲突路数。
时间比在那台机器上是 9.83×，与这里的 10.1× 同量级——
但要注意这是两台不同的 GPU，能对上的是机制，不是绝对数值。
:::

理论上界是 32×（一次广播变成 32 次串行事务），实测 10× 是因为循环本身的加法、
索引计算等开销稀释了。10 倍已经足够说明问题：
**一个 stride 写错，kernel 的访存部分慢一个数量级。** 这正是 L2.4 里 swizzle 布局要消灭的东西。

### 带宽-延迟积：需要多少并发才能喂饱

Little's Law：

$$\text{达到的带宽} = \frac{\text{在途字节数}}{\text{延迟}} \quad\Longrightarrow\quad \text{在途字节数} = \text{带宽} \times \text{延迟}$$

代入实测值：

- **5090**：$1674\ \text{GB/s} \times 320.3\ \text{ns} =$ **536 KB** 必须始终在途
- **L40S**：$681\ \text{GB/s} \times 253.4\ \text{ns} =$ **173 KB**

**带宽越高的卡，需要的访存并行度越大。** 这是个容易被忽略的结论：
5090 的带宽是 L40S 的 2.5 倍，但要喂饱它需要 3.1 倍的在途请求。

实测并发扫描印证了这一点：

| blocks/SM | 5090 GB/s | 占峰值 | L40S GB/s | 占峰值 |
|---|---|---|---|---|
| 1 | 1550 | 93% | 651 | 96% |
| 2 | 1680 | 100% | 669 | 98% |
| 4 | 1690 | 101% | 680 | 100% |
| 8 | 1695 | 101% | 681 | 100% |

好消息是：**2 个 block/SM（16 warp）就已经饱和**。因为每个 warp 的一次 `float4` load
就是 32 × 16 B = 512 B 在途，2720 个 warp × 512 B = 1.39 MB ≫ 536 KB。

**但这有个前提：访存是"宽"的（float4）且合并良好。**
在“每个 warp 只有一个这类在途 load”的简化模型中，
改为每线程读取一个 `float` 后，每 warp 从 512 B 降到 128 B，
维持相同在途字节数需要 4 倍的 warp 数。真实执行还受发射率和队列容量约束。
如果访存不合并，情况还要更糟。这就是 L2.3 的核心。

---

## 工程实现 · 源码走读：硬件容量如何约束 tile

L0.1 的 kernel trace 里，prefill 阶段跑的 attention kernel 全名是：

```
void flash::flash_fwd_splitkv_kernel<
    Flash_fwd_kernel_traits<128, 64, 128, 4, false, false, cutlass::bfloat16_t,
                            Flash_kernel_traits<128, 64, 128, 4, cutlass::bfloat16_t> >,
    true, false, false, false, ...>
```

按 FlashAttention-2 的 traits 约定，前四个参数是
`<kHeadDim, kBlockM, kBlockN, kNWarps>`，即 **head_dim=128、Q tile 64 行、KV tile 128 行、4 个 warp**。

第一个参数有个硬交叉验证：Qwen3-1.7B 的 `config.json` 里 `"head_dim": 128`（L0.1 原始现场 ①）。
对上了。

**现在用本章测出的硬件常数解释后三个数字。**

一个 tile 要在共享内存里放下 Q、K、V 三块（bf16，2 字节）：

$$\underbrace{64 \times 128 \times 2}_{Q\ =\ 16\ \text{KB}} + \underbrace{128 \times 128 \times 2}_{K\ =\ 32\ \text{KB}} + \underbrace{128 \times 128 \times 2}_{V\ =\ 32\ \text{KB}} = 80\ \text{KB}$$

而本机实测 `sharedMemPerBlockOptin = 99 KB`。**80 < 99，刚好放得下。**

如果把 `kBlockN` 从 128 提到 256：$16 + 64 + 64 = 144$ KB > 99 KB，**编译期就会失败**。
如果降到 64：smem 只用 48 KB，但每个 Q tile 要多循环一倍次数，K/V 的重复加载翻倍。

在上面的 Q/K/V 同时驻留假设下，容量可以排除较大的候选配置。
但这不是对源码最优 tile 的证明：缓冲复用、流水级数、寄存器布局与调度都会改变选择。
应先核对实际 kernel 的共享内存布局，再用容量计算和实测共同判断。

`kNWarps=4` 呢？4 warp = 128 线程。回到带宽-延迟积：
一个 block 要能持续供上访存，同时寄存器又不能爆（每 SM 只有 256 KB 寄存器文件）。
warp 太少喂不饱访存，太多则每线程可用寄存器变少、accumulator 放不下要 spill。

::: note 这是本层存在的意义
「FlashAttention 用 64×128 的 tile」这句话，背下来没有用。
更有用的是知道哪些条件限制候选参数：共享内存容量、寄存器预算和访存并行度。
换硬件、dtype 或 head_dim 时，先重新算可行范围，再比较候选实现。
L3.2 会把 FA1→FA4 每一代的 tile 变化都对应到硬件参数的变化上。
:::

### 另一处：`sharedMemPerBlockOptin` 为什么是 "Optin"

本机实测每 block 最多 99 KB，但**默认只给 48 KB**。超过 48 KB 必须显式申请：

```cpp
cudaFuncSetAttribute(kernel, cudaFuncAttributeMaxDynamicSharedMemorySize, 99 * 1024);
```

历史原因：48 KB 是 Kepler/Maxwell 时代的上限，为了向后兼容，
超出部分要求 kernel 主动"opt in"。**所有现代 attention/GEMM kernel 都会调这个 API**，
忘了调就会在启动时报 `invalid argument` —— 一个很常见又很难查的错误。

而且 L1 与共享内存是**同一块 128 KiB 物理 SRAM 分割出来的**。
申请 99 KB 共享内存，L1 就只剩 29 KB。这解释了实测里的一个现象：
**128 KiB 工作集的指针追逐已经掉到 285 周期**，而不是 L1 的 44 周期——
因为可用的 L1 数据缓存并没有整整 128 KiB。

---

## 工程实现 · 自己写一遍

`labs/L1/mem_hierarchy.cu`。四个实验，每个都有一个方法学要点：

**A. 测延迟 → 必须用依赖链 + 随机顺序 + 走遍工作集**

```cpp
__global__ void chase_kernel(const uint32_t* __restrict__ buf, int steps,
                             uint32_t* out, long long* cycles) {
    uint32_t idx = 0;
    for (int i = 0; i < steps; ++i) idx = buf[idx];   // 预热
    long long t0 = clock64();
    for (int i = 0; i < steps; ++i) idx = buf[idx];   // ← 每次都依赖上一次
    long long t1 = clock64();
    *out = idx;                                       // 防止整个循环被优化掉
    *cycles = t1 - t0;
}
```

三个必须做对的地方，缺一个结论就是假的：

```cpp
const size_t LINE = 128;                    // ① 每个链节点占一整条 cache line
size_t n_lines = bytes / LINE;
std::shuffle(order.begin() + 1, order.end(), rng);   // ② 随机，打掉硬件预取器
size_t steps = std::max((size_t)min_steps, n_lines * 3);  // ③ 走遍整个工作集 3 遍
```

::: warning 第 ③ 条是踩出来的
第一版对所有 buffer 都固定走 4096 步。对 1 GiB 的 buffer，
4096 次随机访问只碰到 4096 条 cache line ≈ 512 KB —— **整个"工作集"一直待在 L2 里**。
结果 1 MiB 到 1 GiB 全测出 360 周期，L2 与显存的差别凭空消失。

改成 `steps = n_lines × 3` 之后，三段平台立刻显现。
**"曲线是平的"往往不是硬件没差别，而是你的实验没触碰到那个差别。**
:::

**B. 时钟自校准 → 不要相信 `cudaDevAttrClockRate`**

`clock64()` 数的是真实 SM 周期，但要换算成纳秒就得知道频率。
`cudaDevAttrClockRate` 报的是**基频**（5090 是 2.41 GHz），而 GPU 会 boost。
自校准的做法是同时用两种计时器：

```cpp
CHECK(cudaEventRecord(ea));
chase_kernel<<<1, 1>>>(...);
CHECK(cudaEventRecord(eb));
CHECK(cudaEventElapsedTime(&ms, ea, eb));      // 墙钟
CHECK(cudaMemcpy(&cyc, d_cyc, ...));           // 周期
double ghz = (double)(cyc * 2) / (ms * 1e6);   // 两者相除 = 本次运行的实际频率
```

标定实测：5090 得 **2.90 GHz**（基频 2.41，最大 boost 3.09 —— 落在区间内，合理）；
L40S 得 2.51 GHz（基频 2.52）。

这里也有个坑：**标定 kernel 必须够长**。第一版用 4096 步（约 0.1 ms），
kernel launch 的几微秒被算进 event 时间，把频率压到 1.45 GHz —— 低了整整一倍。
改成 200 万步（约 0.1 秒）后才稳定。

**C / D 的要点**已在上面机制部分讲过：测吞吐要用独立操作；
测饱和带宽要用远超 L2 的工作集且只扫一遍。

---

## 动手 lab

```bash
nvcc -O3 -std=c++17 -arch=sm_120 -o mem_hierarchy labs/L1/mem_hierarchy.cu
./mem_hierarchy
```

`-arch` 必须匹配你的卡（5090 是 `sm_120`，L40S 是 `sm_89`）。
架构不匹配时 nvcc 不会报错，驱动会 JIT PTX，性能特征可能变化（L2.2 详解）。

### 建议你做的消融

| 改什么 | 你应该看到什么 |
|---|---|
| 把 `LINE` 从 128 改成 32 | 每条 cache line 上放 4 个链节点 → 实际触碰的字节数只有 1/4，**三段平台会退化**。这能让你亲手复现我踩的那个坑 |
| 把 `std::shuffle` 去掉（顺序链） | 硬件预取器生效，显存延迟会大幅"变快"——测出的是预取后的假延迟 |
| `smem_kernel` 里 stride 试 2/4/8/16 | 冲突倍数应该大致按 2/4/8/16 递增（部分冲突） |
| `bw_read_kernel` 里把 `float4` 换成 `float` | 每 warp 在途字节从 512 B 掉到 128 B，**饱和需要的 blocks/SM 应该涨约 4 倍** |
| 在 worldvln 上改 `CUDA_VISIBLE_DEVICES` 到 3 | GPU3 在另一个 NUMA 节点，观察是否影响（应该不影响显存延迟，只影响 H2D —— 见 L1.3） |

倒数第二行是本章最值得做的一个实验：它把「访存宽度」和「所需并发」这两个概念绑在一起。

---

## 原始现场

<details markdown="1">
<summary>① 5090 的完整输出</summary>

```
=== NVIDIA GeForce RTX 5090 D  sm_120  SM=170  L2=96.0 MiB  显存=33.7 GB  SM时钟=2.41 GHz
    每 SM 最大线程 1536，每 block 最大共享内存 99 KB，warp 大小 32
    标定：基频 2.41 GHz，本次运行有效 2.90 GHz（clock64 与 event 交叉反推）

[A] 指针追逐延迟（随机环，单线程，无法并行 ⇒ 纯延迟）
    工作集    周期/次   纳秒/次   推测命中层级
    4 KiB        44.1         15.22        L1
    16 KiB       44.1         15.22        L1
    32 KiB       44.1         15.22        L1
    64 KiB       44.1         15.21        L1
    128 KiB      285.0        98.24        L1
    256 KiB      360.2        124.16       L2
    512 KiB      360.0        124.08       L2
    1 MiB        360.0        124.10       L2
    4 MiB        359.9        124.06       L2
    16 MiB       359.9        124.05       L2
    32 MiB       359.9        124.05       L2
    64 MiB       359.8        124.04       L2
    96 MiB       575.6        198.43       L2（已到容量边界）
    128 MiB      929.2        320.31       显存
    256 MiB      929.0        320.25       显存
    512 MiB      934.2        322.03       显存

[B] 只读带宽 vs 工作集
    参考边界：L1 聚合约 21 MiB（170 SM × 128 KiB），L2 = 96 MiB
    1 MiB        10427.1        L1（各 SM 私有）
    4 MiB        24070.6        L1（各 SM 私有）
    16 MiB       27016.5        L1（各 SM 私有）
    32 MiB       12866.0        L2
    64 MiB       5513.4         L2
    80 MiB       5412.5         L2
    96 MiB       5271.0         L2
    128 MiB      3005.6         显存
    192 MiB      2091.2         显存
    256 MiB      1976.6         显存
    512 MiB      1677.9         显存
    1024 MiB     1674.0         显存

[C] 共享内存延迟与 bank conflict
    stride=1  （32 线程分散在 32 个 bank）：6.51 周期/次访存
    stride=32 （32 线程全撞同一个 bank）  ：65.79 周期/次访存
    冲突倍数 10.1×

[D] 并发度 vs 带宽（1 GB 工作集，单遍扫描，block=256）
    blocks/SM   常驻 warp  带宽 GB/s  占已测峰值
    1           1360         1550.3       93%   已饱和
    2           2720         1680.2       100%  已饱和
    4           5440         1689.5       101%  已饱和
    8           8160         1695.4       101%  已饱和
    16          8160         1690.2       101%  已饱和
    参照：实验 B 在 1 GB 工作集下测得 1673 GB/s
```

原始文件：`results/crater/mem_hierarchy_5090.txt`
</details>

<details markdown="1">
<summary>② L40S 的对照输出（同一份 .cu，只换 -arch）</summary>

```
=== NVIDIA L40S  sm_89  SM=142  L2=96.0 MiB  显存=47.7 GB  SM时钟=2.52 GHz
    标定：基频 2.52 GHz，本次运行有效 2.51 GHz

[A]  4–64 KiB   40.0 周期 / 15.91 ns    L1
     128 KiB    222.1     / 88.38       L1 边界
     256K–32M   279.0     / 111.03      L2
     64 MiB     284.0     / 113.02      L2
     96 MiB     354.2     / 140.96      L2 容量边界
     128–512M   ~636      / ~253        显存

[B]  16 MiB     19395.6 GB/s   L1
     64–96 MiB  ~4270          L2
     1024 MiB   681.4          显存

[C]  stride=1  6.65 周期 · stride=32  65.55 周期 · 冲突 9.9×

[D]  1 blk/SM 651 GB/s (96%) → 4 blk/SM 680 GB/s (100%)
```

两张卡的 L2 都是 96 MiB、每 SM smem 上限都是 99 KB、bank 冲突倍数都是 ~10×——
**这些是跨两代架构不变的量**。变的是延迟（5090 各级都更高）和带宽（5090 各级都更高）。

原始文件：`results/worldvln/mem_hierarchy_l40s.txt`
</details>

<details markdown="1">
<summary>③ 设备属性原文（哪些数字可以直接读，哪些必须测）</summary>

```
name                          NVIDIA GeForce RTX 5090 D
compute capability            12.0            ← 决定能用哪些指令（L1.2）
multiProcessorCount           170
l2CacheSize                   100663296       ← 96 MiB，可直接读
maxThreadsPerMultiProcessor   1536            ← 48 warp
sharedMemPerBlockOptin        101376          ← 99 KB，决定 tile 上界
warpSize                      32
clockRate                     2410000 kHz     ← 基频，不是运行频率
totalGlobalMem                33.7 GB
```

**可以直接读的**：SM 数、L2 容量、smem 上限、每 SM 线程上限、warp 大小。
**必须测的**：各级延迟、各级带宽、实际运行频率、bank 冲突代价、饱和所需并发。

第二组里没有一个能从 API 或规格表拿到，而它们恰恰是决定 kernel 写法的那些数。
</details>

<details markdown="1">
<summary>④ 那个真实的 attention kernel 全名</summary>

```
void flash::flash_fwd_splitkv_kernel<
    Flash_fwd_kernel_traits<128, 64, 128, 4, false, false, cutlass::bfloat16_t,
                            Flash_kernel_traits<128, 64, 128, 4, cutlass::bfloat16_t> >,
    true, false, false, false, false, true, false, false>(
        Flash_fwd_params)
```

C++ 模板实例化的名字是**信息量最大的调试线索之一**：不用读源码，
就能从名字里读出 head_dim、tile 尺寸、warp 数、数据类型、以及一串编译期开关。
`splitkv` 说明这是 split-K 变体（把 KV 序列切成多段并行，最后再 combine —— L3.3）。

L0.1 的 trace 里还看到配套的 `flash_fwd_splitkv_combine_kernel`，
就是那个 combine 步骤，占 decode GPU 时间的 2.5%。
</details>

---

## 前沿

| 方向 | 与本章的关系 |
|---|---|
| **异步拷贝与 TMA** | Ampere 的 `cp.async` 让"搬数据到 smem"不占寄存器、不阻塞计算；Hopper 的 TMA 进一步把整块搬运交给专用硬件。本质都是在提高"在途字节数"以逼近带宽-延迟积（L1.2、L2.4） |
| **分布式共享内存（DSMEM）** | Hopper 起，同一 cluster 内的 block 可以互相访问 smem，等于把有效 smem 容量放大到 cluster 级。这直接放宽了本章算出的 99 KB 上界 |
| **Tensor Memory（TMEM）** | sm_100 独有的、tensor core 专用的片上存储，把累加器从寄存器文件里搬出去。**sm_120 没有**（L1.2） |
| **L2 驻留控制** | `cudaStreamSetAttribute` 的 access policy window 可以把某段显存"钉"在 L2 里。对 KV cache 的热前缀理论上有用，但目前主流引擎都没用（L5.2 的一个开放方向） |
| **FlashAttention-4 的软件仿真 exp()** | 因为 `MUFU`（超越函数单元）的吞吐跟不上 Blackwell 的 tensor core，FA4 改用多项式近似在 FMA 单元上算 exp。这是"某一级硬件资源成为瓶颈后，算法绕过它"的典型（L3.2） |

> 除本站实测外的内容来自各自文档与论文，**未在本站复现**<span class="unverified">未实测</span>。

---

## 陷阱

**1. 用顺序访问测延迟。** 硬件预取器会把顺序访问的延迟完全掩盖，测出的显存延迟可能只有真实值的 1/5。必须随机。

**2. 工作集没走遍。** 本章踩过：固定步数的指针追逐，对大 buffer 而言只触碰了很小一部分，三段平台会消失。

**3. 用依赖链测吞吐 / 用独立操作测延迟。** 两者测的是不同的东西，用反了会得到"没有效应"的假结论。本章的 bank conflict 实验就是这么翻车的。

**4. 相信 `cudaDevAttrClockRate`。** 那是基频。要么自校准，要么只报周期数不报纳秒。

**5. 标定 kernel 太短。** launch 开销几微秒，对 0.1 ms 的 kernel 是 5% 误差，对更短的就是数倍误差。

**6. 忘了 smem 和 L1 是同一块 SRAM。** 申请 99 KB smem 后 L1 只剩 29 KB。这解释了为什么 128 KiB 工作集的延迟已经不是 L1 的 44 周期。

**7. 把"占用率高"当成"性能好"。** 实测 2 个 block/SM 就饱和了带宽，再往上加没有收益。占用率只是掩盖延迟的**手段**，不是目标（L2.6 会展开 occupancy vs ILP 的取舍）。

---

## 自测题

<details markdown="1">
<summary>1. 我的 kernel 每个线程只 load 一个 <code>float</code>（4 字节），访存完全合并。要打满 5090 的显存带宽，需要多少个 warp 同时在跑？</summary>

带宽-延迟积 = 1674 GB/s × 320.3 ns ≈ **536 KB** 必须在途。

每个 warp 的一次合并 `float` load = 32 线程 × 4 B = **128 B**。

假设每个 warp 同时只有 1 个未完成的 load：

$$536\,\text{KB} / 128\,\text{B} = 4288\ \text{个 warp}$$

而全卡最多常驻 170 SM × 48 warp = **8160 个 warp**。所以需要 **52% 的占用率**。

如果换成 `float4`（每 warp 512 B），只要 1072 个 warp = 13% 占用率就够了——
这就是为什么向量化访存（`float4`/`LDG.128`）几乎是所有 memory-bound kernel 的第一步优化。

**注意这个估算的假设**：每 warp 只有 1 个在途 load。真实情况下编译器会展开循环、
同时发多个独立 load，所需 warp 数会更少。实测（[D]）显示 2 blocks/SM 即饱和，
正是因为编译器为 `float4` 循环做了展开。
</details>

<details markdown="1">
<summary>2. 为什么 5090 的延迟每一级都比 L40S 高，带宽却高 2.5 倍？这对 LLM 推理意味着什么？</summary>

GDDR7 用**更多的 bank、更深的流水、更高的信号速率**换带宽，代价是单次访问的往返路径更长。
L2 变大（同为 96 MiB 但 Blackwell 的分片更多、跨片距离更远）也会增加延迟。
**这是带宽与延迟的经典取舍，不是"5090 更差"。**

对 LLM 推理的影响：

- **decode**：几乎不受影响。decode 是大规模并行访存（几千个 warp 同时读权重和 KV），
  延迟被并发完全掩盖，只有带宽重要。所以 5090 在 decode 上快 2.48×（L0.3 实测）。
- **有长依赖链的小 kernel**：受影响。比如采样里的某些串行归约、
  block table 的指针追逐、以及 CPU-GPU 同步点附近的小操作。
- **一般规律**：现代 GPU 的设计假设就是"你有足够多的并行度"。
  如果你的负载没有，你拿不到它标称的任何好处。
</details>

<details markdown="1">
<summary>3. FlashAttention 的 tile 是 <code>&lt;head_dim=128, BlockM=64, BlockN=128, 4 warps&gt;</code>。如果我用一个 head_dim=256 的模型，tile 会怎么变？为什么？</summary>

smem 需求变成：

$$\underbrace{64 \times 256 \times 2}_{Q = 32\,\text{KB}} + \underbrace{128 \times 256 \times 2}_{K = 64\,\text{KB}} + \underbrace{128 \times 256 \times 2}_{V = 64\,\text{KB}} = 160\ \text{KB}$$

**远超 99 KB 上限**，编译不过。必须缩小 tile，通常是把 `kBlockN` 砍到 64：

$$32 + 32 + 32 = 96\ \text{KB} < 99\ \text{KB}\quad\checkmark$$

**代价**：KV tile 减半意味着每个 Q tile 要循环两倍次数，K/V 从 L2/显存重复加载的量翻倍。
所以 **head_dim 越大，attention 的实际效率越低**——这是一个纯粹由 smem 容量决定的硬约束。

（这也解释了为什么大部分模型的 head_dim 停在 128：再大就打不满 tensor core 又装不下 tile。
DeepSeek 的 MLA 之所以能用 576 维的压缩 KV，是因为它的 kernel 是专门重写的，
不是通用 FlashAttention —— L3.3。）
</details>

<details markdown="1">
<summary>4. 我把 GEMM 的 tile 从 128×128 改成 64×64，smem 用量降到 1/4，占用率翻倍。性能会变好吗？</summary>

**大概率变差。** 因为 tile 变小会让**数据复用率**下降。

对 $M\times K$ 乘 $K\times N$ 的 tile GEMM，一个 $T\times T$ 的输出 tile 需要读入 $2TK$ 个元素，
产出 $T^2$ 个结果，算术强度 $\propto T$。tile 边长减半 → 算术强度减半 → 访存量翻倍。

用本章的数字：L1 与显存的带宽比是 16:1。tiling 的全部价值就是把复用留在 smem 里。
tile 变小 = 少复用 = 更多流量掉回 L2/显存。

而占用率翻倍带来的收益是有限的——[D] 实测显示 2 blocks/SM 就已经饱和带宽，
**更高的占用率买不到更多带宽**。

**这就是 occupancy 的经典误区**：占用率是掩盖延迟的手段，一旦延迟已经被掩盖住，
再提高它只会挤占寄存器和 smem，反而伤害每线程的工作效率（ILP）。L2.6 会用 `ncu` 量化这个取舍。
</details>

<details markdown="1">
<summary>5. 用本章的数字估算：Qwen3-1.7B 在 batch=1、ctx=1024 时 decode 一步，理论上要多少纳秒？和 L0.3 实测的 4.48 ms 差多少？</summary>

L0.2 算过：这一步要搬 3.441 GB 权重 + 0.117 GB KV = **3.558 GB**。

按本章实测的显存带宽 1674 GB/s：

$$3.558\ \text{GB} / 1674\ \text{GB/s} = 2.126\ \text{ms}$$

（L0.2 用的是 `probe_hw.py` 的 copy 带宽 1519 GB/s，得 2.34 ms。两个分母都合理，
差别在于 copy 计入读+写、本章只读。**只读带宽是更贴切的分母**，因为 decode 主要是读权重。）

实测 GPU 忙时 3.085 ms，墙钟 4.48 ms。

拆解差距：
- 2.126 ms → 3.085 ms（GPU 忙时）：**效率 69%**。差的部分是 kernel 效率、
  非 GEMM 小算子、以及 attention 那部分不是纯顺序读。
- 3.085 ms → 4.48 ms（墙钟）：**1.4 ms 的 CPU launch 开销**（444 次 × 2.68 µs ≈ 1.19 ms）。

**能把 4.48 ms 拆成 2.13 + 0.96 + 1.40 三段，才算真的懂了这一步在干什么。**
每一段对应一层优化：带宽（换卡/量化）、kernel 效率（L2）、launch 开销（CUDA Graph，L5.4）。
</details>
