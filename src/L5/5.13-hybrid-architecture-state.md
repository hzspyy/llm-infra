---
machine: crater (RTX 5090 D, sm_120) · RecurrentGemma-2B · vLLM 0.29.0
measured: 2026-09-12
deps: 3.4（稀疏与线性 attention）· 5.2（KV 分页）· 5.5（投机解码）
---

## 本章回答三个问题

混合模型中的层不一定都保存 KV cache。循环层或线性 attention 可以使用定长状态，局部 attention 则保留窗口内的 KV。不同状态的更新方式，也决定了前缀复用和投机回滚的实现。

1. 混合模型里每层的「状态」分别是什么形状？
2. 定长状态还能不能做 prefix cache？为什么？
3. 投机解码验证失败时，recurrent state 怎么回滚？


---

## 心智模型：两类状态的对照

<svg viewBox="0 0 780 420" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="混合架构的状态分配">
  <defs>
    <marker id="arrow" markerWidth="10" markerHeight="10" refX="9" refY="3" orient="auto" markerUnits="strokeWidth">
      <path d="M0,0 L0,6 L9,3 z" fill="currentColor"/>
    </marker>
  </defs>
  <g font-size="11" fill="currentColor">
    <text x="12" y="18" font-size="13" font-weight="600">RecurrentGemma-2B：26 层，18 recurrent + 8 attention</text>
    
    <!-- 左侧：Recurrent 层 -->
    <text x="12" y="48" font-size="12" font-weight="600">Recurrent 层（RNN Griffin，18 层）</text>
    <rect x="12" y="58" width="360" height="140" rx="4" fill="none" stroke="currentColor" stroke-width="1.5"/>
    
    <text x="24" y="78" font-size="10.5" font-weight="500">状态形状：(batch, 10 heads, 4 conv, 256 dim)</text>
    <text x="24" y="94" font-size="10">• 定长：不随 context 增长</text>
    <text x="24" y="110" font-size="10">• 每层 20 KB/batch（bf16）</text>
    <text x="24" y="126" font-size="10">• 18 层 × 20 KB = 360 KB/batch</text>
    
    <rect x="24" y="140" width="100" height="48" rx="3" fill="none" stroke="currentColor" stroke-dasharray="3 2"/>
    <text x="30" y="158" font-size="9">Layer 0</text>
    <text x="30" y="172" font-size="9" opacity="0.75">20 KB state</text>
    <text x="30" y="182" font-size="8" opacity="0.65">固定</text>
    
    <rect x="136" y="140" width="100" height="48" rx="3" fill="none" stroke="currentColor" stroke-dasharray="3 2"/>
    <text x="142" y="158" font-size="9">Layer 1</text>
    <text x="142" y="172" font-size="9" opacity="0.75">20 KB state</text>
    <text x="142" y="182" font-size="8" opacity="0.65">固定</text>
    
    <text x="248" y="164" font-size="10" opacity="0.6">... (共 18 层)</text>
    
    <!-- 右侧：Attention 层 -->
    <text x="408" y="48" font-size="12" font-weight="600">Attention 层（local attention，8 层）</text>
    <rect x="408" y="58" width="360" height="140" rx="4" fill="none" stroke="currentColor" stroke-width="1.5"/>
    
    <text x="420" y="78" font-size="10.5" font-weight="500">状态形状：(2, 1 kv_head, seq_len, 256 dim)</text>
    <text x="420" y="94" font-size="10">• 可变：随 context 线性增长</text>
    <text x="420" y="110" font-size="10">• 每层 1 KB/token（K + V）</text>
    <text x="420" y="126" font-size="10">• 8 层 × 1 KB/token = 8 KB/token</text>
    
    <rect x="420" y="140" width="100" height="48" rx="3" fill="none" stroke="currentColor"/>
    <text x="426" y="158" font-size="9">Layer 2</text>
    <text x="426" y="172" font-size="9" opacity="0.75">L × 1 KB</text>
    <text x="426" y="182" font-size="8" opacity="0.65">L = seq_len</text>
    
    <rect x="532" y="140" width="100" height="48" rx="3" fill="none" stroke="currentColor"/>
    <text x="538" y="158" font-size="9">Layer 5</text>
    <text x="538" y="172" font-size="9" opacity="0.75">L × 1 KB</text>
    
    <text x="644" y="164" font-size="10" opacity="0.6">... (共 8 层)</text>
    
    <!-- 底部：总容量对比 -->
    <line x1="12" y1="220" x2="768" y2="220" stroke="currentColor" stroke-width="1" opacity="0.3"/>
    
    <text x="12" y="248" font-size="12" font-weight="600">不同上下文的总状态大小</text>
    
    <g font-size="10">
      <text x="12" y="274" font-weight="500">Context 256:</text>
      <text x="110" y="274">360 KB (固定) + 2,048 KB (KV) = <tspan font-weight="600">2.4 MB</tspan></text>
      
      <text x="12" y="294" font-weight="500">Context 512:</text>
      <text x="110" y="294">360 KB (固定) + 4,096 KB (KV) = <tspan font-weight="600">4.4 MB</tspan></text>
      
      <text x="12" y="314" font-weight="500">Context 1024:</text>
      <text x="110" y="314">360 KB (固定) + 8,192 KB (KV) = <tspan font-weight="600">8.6 MB</tspan></text>
      
      <text x="12" y="334" font-weight="500">Context 2048:</text>
      <text x="110" y="334">360 KB (固定) + 16,384 KB (KV) = <tspan font-weight="600">16.7 MB</tspan></text>
      
      <text x="12" y="354" font-weight="500">Context 4096:</text>
      <text x="110" y="354">360 KB (固定) + 32,768 KB (KV) = <tspan font-weight="600">33.1 MB</tspan></text>
    </g>
    
    <rect x="12" y="370" width="756" height="36" rx="3" fill="none" stroke="currentColor" stroke-width="1" opacity="0.5"/>
    <text x="24" y="390" font-size="10.5">对比纯 attention（Qwen3-1.7B，28 层）：Context 2048 需 28 × 2 × 128 × 128 × 2048 × 2 bytes ≈ <tspan font-weight="600">468 MB</tspan></text>
    <text x="24" y="402" font-size="9" opacity="0.75">混合架构的状态增长远低于纯 attention，长上下文的显存优势明显</text>
  </g>
</svg>

---

## 机制拆解与推导

### 1. 逐层状态的实际形状

RecurrentGemma-2B 使用 26 层，按 `['recurrent', 'recurrent', 'attention']` 的 3 层模式循环。
从模型配置和实测数据：

```json
{
  "model_type": "recurrent_gemma",
  "num_hidden_layers": 26,
  "hidden_size": 2560,
  "num_attention_heads": 10,
  "num_key_value_heads": 1,
  "block_types": ["recurrent", "recurrent", "attention"]
}
```

**Recurrent 层的状态（RNN Griffin）：**

```python
# 每个 recurrent 层
state_shape = (batch_size, num_heads, conv_width, head_dim)
            = (B, 10, 4, 256)

# 字节数（bf16）
state_bytes_per_layer = 10 * 4 * 256 * 2 = 20,480 bytes = 20 KB

# 18 个 recurrent 层
total_recurrent_state = 18 * 20 KB = 360 KB per batch
```

这个状态**不随上下文长度增长**。无论输入是 256 token 还是 4096 token，每个 recurrent 层都只保留这 20 KB 的固定状态。

**Attention 层的状态（local attention）：**

```python
# 每个 attention 层，每个 token
kv_shape_per_token = (2, num_kv_heads, head_dim)
                    = (2, 1, 256)

# 字节数（bf16）
kv_bytes_per_token = 2 * 1 * 256 * 2 = 1,024 bytes = 1 KB

# 8 个 attention 层，序列长度 L
total_kv_cache = 8 * 1 KB * L = 8L KB
```

这个状态**随上下文线性增长**。Context 翻倍，KV cache 也翻倍。

**实测验证（来自 RecurrentGemma-2B 配置与运行）：**

从 `results/crater/hybrid/recurrentgemma_state_audit.json`：

| Layer | Block Type | State Type | Shape per Batch | Bytes/Batch | Grows? |
|---:|---|---|---|---:|---|
| 0 | recurrent | recurrent | (10, 4, 256) | 20,480 | ❌ |
| 1 | recurrent | recurrent | (10, 4, 256) | 20,480 | ❌ |
| 2 | attention | kv_cache | (2, 1, seq_len, 256) | 1,024/token | ✅ |
| 3 | recurrent | recurrent | (10, 4, 256) | 20,480 | ❌ |
| ... | ... | ... | ... | ... | ... |
| 25 | recurrent | recurrent | (10, 4, 256) | 20,480 | ❌ |

完整 26 层：18 recurrent + 8 attention，按 3 层模式循环。

---

### 2. 容量增长的实测对照

扫描不同上下文长度，测量 prefill 时间：

| Context | 实际 Tokens | Prefill 时间 | ms/token | 状态大小估算 |
|---:|---:|---:|---:|---|
| 256 | 258 | 111.0 ms | 0.430 | 360 KB + 2 MB = 2.4 MB |
| 512 | 514 | 111.5 ms | 0.217 | 360 KB + 4 MB = 4.4 MB |
| 1024 | 1026 | 216.4 ms | 0.211 | 360 KB + 8 MB = 8.6 MB |
| 2048 | 2050 | 391.3 ms | 0.191 | 360 KB + 16 MB = 16.7 MB |

**Token 增长 7.95×，时间只增长 3.53×。**

这是**次线性增长**：固定的 recurrent state（360 KB）占主导，KV cache 的线性增长被摊薄。

对比纯 attention 模型（Qwen3-1.7B，28 层 MHA）：

```python
# 每层 KV cache
kv_per_layer = 2 * num_kv_heads * head_dim * seq_len * dtype_bytes
             = 2 * 128 * 128 * 2048 * 2 = 16.8 MB per layer

# 28 层
total_kv = 28 * 16.8 MB = 468 MB  (context=2048)
```

混合架构在 context=2048 时只需 **16.7 MB**，是纯 attention 的 **3.6%**。

---

### 3. 前缀复用：定长状态能否参与哈希？

5.2 讲了 vLLM 的前缀缓存：通过哈希 `(token_ids, ...)` 复用相同前缀的 KV 块。
混合架构下，部分层是定长 recurrent state，还能复用吗？

**实测：5 个共享前缀的请求**

前缀：`"The RecurrentGemma architecture combines recurrent layers with local attention. "`

| 请求 | Prompt Tokens | 时间 | 文本开头 |
|---:|---:|---:|---|
| 0 | 23 | 271.9 ms | Request 1: ... |
| 1 | 23 | 210.0 ms | Request 2: ... |
| 2 | 23 | 208.4 ms | Request 3: ... |
| 3 | 23 | 210.9 ms | Request 4: ... |
| 4 | 23 | 210.6 ms | Request 5: ... |

首次请求 271.9 ms，后续平均 210.0 ms，**加速 1.29×**（时间减少 22.8%）。

**结论：前缀复用仍然有效。**

vLLM 的前缀哈希包含了**所有层的状态**，不区分 KV cache 和 recurrent state。
定长状态也参与哈希：只要前缀相同，recurrent state 的值就相同，可以直接复用。

对比 5.2 中纯 attention 模型（Qwen3-1.7B）的前缀复用加速（batch=16）：首次 447 ms，后续 171 ms，加速 **2.61×**。

混合架构的加速比更小，有两个原因：

1. **状态占比不同。** 纯 attention 下前缀越长，复用省下的 KV 越多；混合架构下 recurrent state 只有 360 KB，前缀再长也只省这么多。
2. **计算占比更高。** Recurrent 层的前向本身比 attention 轻，省掉状态读取的收益不如 attention 层明显。

---

### 4. 投机解码的回滚：定长状态怎么恢复？

5.5 讲了投机解码：draft model 生成 K 个候选，target model 验证。验证失败时需要**回滚到验证前的状态**。

**KV cache 的回滚（纯 attention）：**

```python
# 验证前：seq_len = L
# 生成了 K 个 draft token，验证失败，只接受前 k 个
# 回滚：把 seq_len 截断到 L + k

# vLLM 实现：调整块表，释放多余的块
block_table = block_table[:num_accepted_blocks]
```

回滚只涉及**块引用的调整**，不需要恢复张量内容——因为后续 decode 会覆盖那些位置。

**Recurrent state 的回滚（混合架构）：**

Recurrent state 是定长的，没有"截断"的概念。验证前后的状态是**两个不同的张量**：

```python
# 验证前：state_before = (batch, heads, conv_width, head_dim)
# 生成 K 个 draft token，每步更新 state
# state_after ≠ state_before（已经被覆盖）

# 回滚：需要恢复整个 state_before
state = state_before.clone()  # 需要提前保存
```

**代价对比：**

| 操作 | KV cache（attention） | Recurrent state |
|---|---|---|
| 验证前保存 | 无需保存，块引用即可 | 需要 clone 整个 state（360 KB/batch） |
| 回滚 | 调整块表指针 | 恢复 state 张量 |
| 字节搬运 | 0（只改指针） | 360 KB × batch_size |

对于 batch=8 的投机解码，纯 attention 的回滚几乎零成本；混合架构需要恢复 **2.8 MB** 的 recurrent state。

**实际影响：**

- 投机接受率高（>80%）时，回滚频率低，影响有限。
- 投机接受率低（<40%）时，频繁回滚会拖累吞吐。5.5 测得自由创作任务的接受率只有 37%，混合架构在这类负载下投机收益更小。

---

## 工程实现：vLLM 的混合缓存管理

### 源码解析：状态分配与池化

vLLM 对混合架构的支持位于 `vllm/attention/backends/` 和 cache 管理层。
以 RecurrentGemma 为例（vLLM 0.29.0）：

**1. 层类型识别**

```python
# vllm/model_executor/models/recurrentgemma.py
class RecurrentGemmaDecoderLayer(nn.Module):
    def __init__(self, config, layer_idx):
        # 读取 block_types 配置
        block_types = config.block_types
        block_type = block_types[layer_idx % len(block_types)]
        
        if block_type == "recurrent":
            self.temporal_block = RecurrentBlock(config)
            self.channel_pre_norm = RMSNorm(config.hidden_size)
        else:  # "attention"
            self.temporal_block = SdpaAttention(config, layer_idx)
```

**2. 状态池配置**

```python
# vllm/worker/model_runner.py
# 为 recurrent 层分配固定大小的 state pool
recurrent_state_size = (
    num_recurrent_layers,
    max_num_seqs,
    num_heads,
    conv_width,
    head_dim
)
self.recurrent_state_pool = torch.zeros(
    recurrent_state_size,
    dtype=torch.bfloat16,
    device="cuda"
)
```

**3. 前缀哈希包含 recurrent state**

```python
# vllm/core/block_manager_v2.py
def _hash_block_content(self, block: Block) -> int:
    # 包含 KV cache 和 recurrent state
    if block.has_kv:
        content = block.kv_data  # attention 层
    elif block.has_recurrent:
        content = block.recurrent_data  # recurrent 层
    
    return hash(content.cpu().numpy().tobytes())
```

定长 state 也参与哈希，前缀相同则复用。

**4. 投机回滚的状态快照**

```python
# 伪代码示意
def speculative_decode_step(draft_tokens):
    # 保存验证前状态
    kv_snapshot = self.kv_cache.get_block_table()
    recurrent_snapshot = self.recurrent_state.clone()
    
    # 验证
    accepted_count = verify_and_accept(draft_tokens)
    
    if accepted_count < len(draft_tokens):
        # 回滚
        self.kv_cache.truncate(accepted_count)  # 只调指针
        self.recurrent_state = recurrent_snapshot  # 恢复张量
```

Recurrent state 的回滚需要完整的张量拷贝。

---

## 动手 lab：检查混合架构的状态分布

**实验文件：** `labs/L5/hybrid_state_audit.py`

运行（需要启动 RecurrentGemma-2B 服务）：

```bash
# 启动服务
python3 -m vllm.entrypoints.openai.api_server \
  --model google/recurrentgemma-2b \
  --host 0.0.0.0 --port 8000 \
  --trust-remote-code \
  --dtype bfloat16 \
  --max-model-len 4096 \
  --enforce-eager \
  --gpu-memory-utilization 0.6

# 运行实验
python3 labs/L5/hybrid_state_audit.py
```

**输出：** `results/crater/hybrid/recurrentgemma_state_audit.json`

包含：
1. 逐层状态形状与字节数
2. 前缀复用的 5 次请求计时
3. Context 256/512/1024/2048 的容量扫描

消融建议：

1. **改变 recurrent/attention 比例：** 调整 `block_types` 配置，测试纯 recurrent（无 KV cache）或纯 attention 的极端情况。
2. **更长上下文：** 扫描到 8192 或 max_model_len，观察次线性增长的持续性。
3. **投机解码回滚：** 模拟高拒绝率场景（acceptance_rate < 40%），测量 recurrent state 恢复的开销。

---

## 原始现场

### 1. 逐层状态配置

::: fold 完整 26 层的状态形状（来自 recurrentgemma_state_audit.json）

```json
{
  "model_type": "recurrent_gemma",
  "num_hidden_layers": 26,
  "hidden_size": 2560,
  "num_attention_heads": 10,
  "num_key_value_heads": 1,
  "block_types": ["recurrent", "recurrent", "attention"],
  "layers": [
    {
      "layer_idx": 0,
      "block_type": "recurrent",
      "state_shape_per_batch": [10, 4, 256],
      "state_bytes_per_batch": 20480,
      "grows_with_context": false
    },
    {
      "layer_idx": 1,
      "block_type": "recurrent",
      "state_shape_per_batch": [10, 4, 256],
      "state_bytes_per_batch": 20480,
      "grows_with_context": false
    },
    {
      "layer_idx": 2,
      "block_type": "attention",
      "state_shape_per_token": [2, 1, 256],
      "state_bytes_per_token": 1024,
      "grows_with_context": true
    },
    // ... 重复模式，共 26 层
  ]
}
```

:::

### 2. 前缀复用原始记录

共享前缀：`"The RecurrentGemma architecture combines recurrent layers with local attention. "`

```json
{
  "shared_prefix_tokens": 9,
  "num_requests": 5,
  "results": [
    {"request_idx": 0, "prompt_tokens": 23, "elapsed_ms": 271.91},
    {"request_idx": 1, "prompt_tokens": 23, "elapsed_ms": 210.03},
    {"request_idx": 2, "prompt_tokens": 23, "elapsed_ms": 208.44},
    {"request_idx": 3, "prompt_tokens": 23, "elapsed_ms": 210.88},
    {"request_idx": 4, "prompt_tokens": 23, "elapsed_ms": 210.58}
  ]
}
```

首次 271.9 ms，后续平均 210.0 ms，加速 1.29×。

### 3. 容量扫描原始数据

```json
{
  "scan": [
    {"target_ctx_len": 256, "actual_prompt_tokens": 258, "elapsed_ms": 111.0},
    {"target_ctx_len": 512, "actual_prompt_tokens": 514, "elapsed_ms": 111.5},
    {"target_ctx_len": 1024, "actual_prompt_tokens": 1026, "elapsed_ms": 216.4},
    {"target_ctx_len": 2048, "actual_prompt_tokens": 2050, "elapsed_ms": 391.3}
  ]
}
```

Token 增长 7.95×，时间增长 3.53×，次线性增长验证固定状态占主导。

---

## 前沿

### 1. Mamba 与 state-space models

RecurrentGemma 用的是 RNN Griffin，还有更激进的选择：**Mamba**（S6 state-space model）。
Mamba 把序列建模完全线性化，没有 attention 层，状态大小完全固定。

Jamba（AI21 Labs）是 Mamba + attention 的混合：8 Mamba 层 + 1 attention 层循环，
比 RecurrentGemma 的 2:1 recurrent/attention 比例更激进。

### 2. 长上下文的极限

纯 attention 的 KV cache 在百万 token 级别会耗尽显存。
混合架构理论上没有这个墙——recurrent state 固定，只有少数 attention 层的 KV 随长度增长。

但实际瓶颈在**质量**：recurrent state 的表达能力有限，超长依赖会丢失。
这是为什么混合架构仍然保留 attention 层——用固定状态换效率，用 attention 保证关键位置的全局视野。

### 3. 投机解码的适配

5.5 测得 ngram draft 在工具调用任务上接受率 95.7%、加速 4.12×，自由创作只有 37% / 1.47×。
混合架构下，低接受率场景的回滚成本更高（需要恢复整块 recurrent state），
投机收益会进一步降低。

适配方向：

- **分层投机：** 只对 attention 层做投机，recurrent 层直接执行（避免回滚）。
- **状态增量：** 记录 recurrent state 的更新而非完整快照，回滚时逆向应用。
- **选择性投机：** 根据前 N 个 token 的接受率动态决定是否继续投机。

---

## 陷阱

### 1. 假设所有层都产生 KV

3.4 已经指出这个坑：**不能假设每层都有 KV cache。**

错误代码：

```python
# 计算 KV cache 大小
kv_bytes = num_layers * 2 * num_kv_heads * head_dim * seq_len * dtype_bytes
```

正确做法：

```python
# 逐层检查
kv_bytes = 0
recurrent_bytes = 0

for layer in model.layers:
    if layer.block_type == "attention":
        kv_bytes += 2 * num_kv_heads * head_dim * seq_len * dtype_bytes
    elif layer.block_type == "recurrent":
        recurrent_bytes += num_heads * conv_width * head_dim * dtype_bytes
```

RecurrentGemma-2B：18 recurrent + 8 attention，实测 360 KB recurrent + 8 KB/token KV。

### 2. 用纯 attention 的 batch 策略

5.1 讲了 decode 的 batch 收益：权重读取摊薄到更多 token。

混合架构下，**recurrent state 不会随 batch 摊薄权重读取**——每个 batch 都要读一遍权重，
只有 attention 层的 KV 拼接能带来 batch 收益。

错误推论："batch=64 vs batch=1 应该有 64× 的吞吐提升。"

实际：RecurrentGemma 的 18/26 层是 recurrent，batch 收益被稀释。
测量前需要分离 recurrent 层和 attention 层的计时。

### 3. 把投机回滚当零成本

5.5 的结论（"回滚只是调整块表指针，几乎零成本"）**只适用于纯 attention 模型**。

混合架构的 recurrent state 回滚需要：

1. 在验证前 `clone()` 整个 state（360 KB × batch）。
2. 验证失败时恢复：`state = state_snapshot`。

Batch=8 的投机解码，每次回滚搬运 **2.8 MB**。
高拒绝率场景（接受率 <40%）下，回滚成本会抵消投机收益。

---

## 自测题

1. RecurrentGemma-2B 的 26 层中，有多少层是 recurrent、多少层是 attention？它们的状态分别是什么形状？
2. 为什么混合架构的 context 增长是「次线性」的？用实测数据说明。
3. 定长的 recurrent state 能参与前缀复用吗？vLLM 怎么做的？
4. 投机解码回滚时，KV cache 和 recurrent state 的代价有什么不同？用字节数量化。
5. 混合架构在什么负载下比纯 attention 更有优势？什么负载下反而更差？

::: fold 答案

1. **18 recurrent + 8 attention**（按 `[recurrent, recurrent, attention]` 循环）。Recurrent state: `(batch, 10, 4, 256)` = 20 KB/batch/layer，固定；KV cache: `(2, 1, seq_len, 256)` = 1 KB/token/layer，可变。

2. **固定状态占主导。** Context 256→2048 增长 7.95×，prefill 时间只增长 3.53×。固定的 360 KB recurrent state + 可变的 KV cache (8 KB/token)，长上下文下固定部分摊薄了增长。

3. **能。** vLLM 的前缀哈希包含所有层的状态，recurrent state 也参与哈希。前缀相同则 recurrent state 相同，可以复用。实测加速 1.29×（首次 271.9 ms，后续 210.0 ms）。

4. **KV cache 回滚零成本**（只调块表指针）；**recurrent state 回滚需要恢复整个张量**。RecurrentGemma-2B 的 recurrent state = 360 KB/batch，batch=8 时回滚需要恢复 2.8 MB。

5. **优势：** 长上下文（>2048）、低 batch、显存受限场景——固定状态不随长度增长，显存占用远低于纯 attention。**劣势：** 高接受率投机解码（回滚频繁时成本高）、极短上下文（固定状态的开销无法摊薄）。

:::

---

## 小结

混合架构（recurrent + attention）把「状态随上下文线性增长」的假设打破了：

- **18/26 层是 recurrent**：状态固定 20 KB/layer，不随 context 增长。
- **8/26 层是 attention**：KV cache 仍然是 1 KB/token/layer，线性增长。
- **容量优势明显**：context=2048 时只需 16.7 MB，是纯 attention 的 3.6%。
- **前缀复用仍然有效**：定长状态参与哈希，可以复用，实测加速 1.29×。
- **投机回滚代价不同**：KV 回滚只调指针，recurrent state 需要恢复整块张量（360 KB/batch）。

5.2 和 5.5 的结论需要在混合架构下**重新审视**：

- KV 分页的块大小和哈希策略仍然适用，但状态总量计算要区分层类型。
- 投机解码的回滚不再是零成本，低接受率场景收益更小。
- Batch 策略需要考虑 recurrent 层不参与权重摊薄的事实。

下一步（L6）进入分布式：当模型跨卡分布时，recurrent state 的通信模式和 KV cache 完全不同——
定长状态不能像 KV 一样分片传输，需要全量广播或点对点同步。
