---
machine: crater (RTX 5090 D, sm_120) · vLLM 0.29.0 · Qwen3-1.7B
measured: 2026-09-11
deps: 3.3（decode attention）· 5.1（prefill 与 decode）· 5.4（执行层）
---

## 本章回答三个问题

投机解码先生成候选 token，再让目标模型并行验证，减少生成同样长度所需的串行迭代。它是否更快，取决于接受长度能否抵消草稿、验证与状态管理的成本。

1. 一次验证可以提交多少 token，收益怎样计算？
2. 贪心验证与随机采样的验证有何不同，怎样保持目标分布？
3. 草稿长度、任务与并发怎样影响收益，已有实验能支持哪些判断？

## 心智模型：只提交通过验证的前缀

<figure>
<svg viewBox="0 0 740 220" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="投机解码中的草稿、验证和状态回退">
<g fill="currentColor" font-size="14">
<rect x="12" y="25" width="190" height="65" rx="5" fill="none" stroke="currentColor"/>
<text x="107" y="51" text-anchor="middle">已提交前缀</text><text x="107" y="75" text-anchor="middle">token 与目标 KV 一致</text>
<path d="M204 58 H252" stroke="currentColor" fill="none"/><text x="230" y="48" text-anchor="middle">→</text>
<rect x="255" y="25" width="225" height="65" rx="5" fill="none" stroke="currentColor"/>
<text x="367" y="51" text-anchor="middle">草稿提出 k 个候选</text><text x="367" y="75" text-anchor="middle">目标模型并行计算分布</text>
<path d="M482 58 H527" stroke="currentColor" fill="none"/><text x="505" y="48" text-anchor="middle">→</text>
<rect x="530" y="25" width="195" height="65" rx="5" fill="none" stroke="currentColor"/>
<text x="627" y="51" text-anchor="middle">顺序检查接受条件</text><text x="627" y="75" text-anchor="middle">遇到首个拒绝即停止</text>
<text x="24" y="135">例：草稿 [a, b, c, d]，前两项通过 → 提交 [a, b, 修正 token]</text>
<text x="24" y="165">未接受的后缀不进入正式输出；目标 KV 与草稿状态按接受长度回退。</text>
<text x="24" y="195">全接受时可再提交一个目标 token；EOS、长度上限可能提前结束。</text>
</g></svg>
<figcaption>并行的是目标前向，不是无条件提交全部草稿。接受长度决定输出和缓存的有效边界。</figcaption>
</figure>

## 机制拆解与推导

### 1. 接受前缀长度决定每轮进展

设一次提出 k 个草稿，连续接受的长度为 A。忽略 EOS 和输出长度截断，一轮通常前进 A+1 个 token：首个拒绝位置生成修正 token；全接受时生成额外的目标 token。

令第 i 个草稿在前 i−1 个都通过的条件下被接受的概率为 a_i，则

$$E[N]=1+\sum_{i=1}^{k}\Pr(A\ge i)
=1+\sum_{i=1}^{k}\prod_{j=1}^{i}a_j.$$

只有把各位置条件接受概率近似为同一个 p，才能简化为

$$E[N]=\sum_{i=0}^{k}p^i=\frac{1-p^{k+1}}{1-p},\quad p\ne1.$$

p=1 时取 k+1。**日志中的“接受 token 总数 / 草稿 token 总数”不是这组条件概率的直接替代品。** 变长草稿、位置相关性和末尾截断都会影响二者的关系。

### 2. 一轮成本必须包含草稿与状态处理

令普通 decode 单步耗时为 T_d，投机一轮的平均总耗时为 T_s。在相同负载、忽略启动与 prefill 差异的稳态模型中，

$$\text{speedup}\approx\frac{E[N]T_d}{T_s}=\frac{E[N]}{c},\qquad c=T_s/T_d.$$

T_s 包括草稿生成、目标前向、接受判定、KV 回退和调度。小 batch 下，多个验证位置可以复用权重读取，因此 T_s 未必随 k+1 线性增长；这只是收益机会，不是“验证几乎免费”的保证。

例如**假设** p=0.957、k=8，则 E[N]≈7.60。这个计算不能用来解释后面的 6.72× 实测：95.7% 来自另一组 k=4 的聚合统计，k=8 实验没有记录逐位置接受概率，也没有单独计时验证阶段。由 7.60/6.72 得到的 1.13 不是实测验证成本。

### 3. 随机验证需要拒绝采样，而不只是逐 token 比对

在贪心解码下，逐个比较草稿与目标 argmax，接受最长匹配前缀即可。随机采样时，设同一前缀下目标分布为 P、草稿分布为 Q，对草稿 x 按下式接受：

$$\alpha(x)=\min\left(1,\frac{P(x)}{Q(x)}\right).$$

拒绝后，从归一化的残差分布采样：

$$R(x)=\frac{\max(P(x)-Q(x),0)}{\sum_y\max(P(y)-Q(y),0)}.$$

接受分支给 x 的概率质量是 min(P(x),Q(x))，拒绝分支补回 max(P(x)−Q(x),0)，相加为 P(x)。若 P=Q，拒绝概率为零，不需要计算全零残差的归一化。

这是理想算术下的分布保证，前提是使用正确的条件分布、接受规则和残差采样。它不保证相同 seed 下产生相同 token 序列；实际低精度计算、停止规则与缓存回退还需单独测试。本章的性能实验使用 temperature=0，不能作为随机采样分布正确性的实验证据。

## 工程实现 · 源码解析

### 实验如何配置目标模型与统计接受量

以下行号对应本章的 lab。它调用 vLLM 0.29.0；这不是 vLLM 核心验证器的逐行审查。

`make_llm()` 固定 eager 执行并关闭前缀缓存，使普通生成与投机生成按同一执行配置比较。启用投机时保留统计：

{{src:labs/L5/speculative.py:45-56}}

`run()` 每个点预热一次、计时一次，以实际返回 token 总数除以调用墙钟时间。它测的是包含 prefill 的完整离线生成调用，不是纯验证 kernel：

{{src:labs/L5/speculative.py:86-96}}

`hook_spec_stats()` 包装 scheduler 的 `make_spec_decoding_stats`，累计草稿和接受数量：

{{src:labs/L5/speculative.py:105-125}}

这段采集代码有两个限制。第一，钩子在预热之前安装，因此接受率计数包含预热和正式调用，而耗时只覆盖后者。第二，在同一 scheduler 上再次安装时会包装已有钩子，后续任务可能重复累计；分子与分母同时重复时比例可能保持，但绝对计数不能直接解释为正式调用的 token 数。补测应单次安装钩子，在预热后清零，并保存逐位置、逐轮统计。

### 草稿来源与后端边界

| 方法 | 草稿来源 | 主要成本与适用条件 | 本章验证范围 |
|---|---|---|---|
| ngram / suffix | 从上下文中查找可续接片段 | 无额外模型权重；依赖实际 token 匹配 | ngram 有性能输出；suffix 为 UNVERIFIED |
| draft model | 独立的小模型 | 草稿前向、权重和 KV；需词表与目标兼容 | UNVERIFIED |
| Medusa / MLP speculator | 额外预测头 | 训练头、构造候选及验证布局 | UNVERIFIED |
| EAGLE 系列 | 利用目标隐藏状态的草稿模块 | 草稿模块前向、状态传递与 KV | UNVERIFIED |
| MTP | 模型配套的多 token 预测模块 | 模型支持、权重加载与运行时集成 | UNVERIFIED |

`custom_class` 是另一类扩展入口：由用户提供草稿实现，而不是一种固定的草稿算法。本章未运行该入口（`UNVERIFIED`）；补测需实现相应接口，并检查候选、接受长度与缓存回退的契约。

`speculative.py` 的 E 组从 `vllm/config/speculative.py` 提取类型字面量。类型列表说明该安装版本声明了哪些配置，不能证明每条路线可运行、占主流或接受率高。SGLang 的提案、验证和回退流程尚缺固定版本源码快照及同负载实测，这是本章的实现对照缺口。

### 设计取舍

- **状态归属。** 调度器持有请求进度；目标与草稿执行路径各自维护所需缓存。接受后，二者必须对同一个已提交前缀达成一致。
- **不变量。** 未验证的后缀不能泄漏到输出；回退后的有效 KV 长度必须与下一轮输入位置匹配。复用内存块之前还要确认引用已释放。
- **草稿长度。** 增大 k 同时增加潜在进展与验证工作量。应在目标并发和上下文下扫描，不能只按平均接受率选择。
- **共享隐藏状态与独立模型。** 前者可减少重复计算，但绑定目标架构与训练产物；后者更容易独立替换，却增加权重、KV 和前向成本。不存在由方法名称决定的固定性能排序。
- **增加后端。** 不只增加一个配置枚举，还要实现提案输入、位置与 mask 布局、随机验证、接受长度回传、缓存回退及停止处理。参数兼容性在初始化检查，前缀和缓存一致性在每轮检查。

上述取舍为机制分析，不代表上游正式否决了某种替代方案。

## 工程实现 · mini 实现

先用一个三分类分布检查拒绝采样的概率质量，不需要 GPU。下列实现枚举所有分支，验证一次提案最终返回的分布，而不是用少量随机样本近似它：

```python
def corrected_distribution(target, draft):
    assert len(target) == len(draft) and target
    assert all(p >= 0 for p in target + draft)
    assert abs(sum(target) - 1) < 1e-12
    assert abs(sum(draft) - 1) < 1e-12
    accepted = [min(p, q) for p, q in zip(target, draft)]
    residual = [max(p - q, 0) for p, q in zip(target, draft)]
    rejected_mass = 1 - sum(accepted)
    if rejected_mass < 1e-12:
        return accepted
    normalizer = sum(residual)
    return [a + rejected_mass * r / normalizer
            for a, r in zip(accepted, residual)]

p = [0.5, 0.3, 0.2]
q = [0.2, 0.5, 0.3]
assert all(abs(a - b) < 1e-12
           for a, b in zip(corrected_distribution(p, q), p))
assert corrected_distribution(p, p) == p
```

把拒绝后的残差改成直接从 P 重采样，再枚举一次，结果通常不再等于 P。完整的多 token 实现还需要按接受前缀更新条件分布；这里没有实现模型前向或 KV 管理。

## 动手 lab

### 运行条件与命令

仅阅读可直接查看下方原始输出。复现实验需要 CUDA GPU、vLLM 0.29.0 及其匹配依赖，并提前准备 Qwen3-1.7B 本地缓存。激活独立环境，在项目根目录运行：

```bash
python labs/L5/speculative.py A B C
```

可通过 `L55_MODEL` 指定模型路径或已缓存的模型标识。运行前检查 GPU 占用和脚本的显存预算；`safe_util()` 中的固定预留及比例仅是历史实验默认值，不是通用安全保证。D 组输出公式示例，E 组读取已安装包的类型定义。历史脚本中夹带的解释性打印不应当作新增证据。

### 同一配置下的两种输入

ngram，k=4，每次返回 200 token。下表数值保留原始记录；接受率统计范围见源码解析。

| 输入 | 无投机 tok/s | 投机 tok/s | 加速 | 聚合接受率 |
|---|---:|---:|---:|---:|
| 抄写 | 217.0 | 893.9 | 4.12× | 95.7%（314/328） |
| 自由创作 | 221.0 | 324.3 | 1.47× | 37.0%（296/800） |

两种输入的接受率和收益不同，但接受率比不是加速比；草稿长度、查找成本、验证时间和 prefill 都参与总耗时。这两个 prompt 也不能代表文档问答、代码生成或推理任务的完整分布。

### 并发与草稿长度扫描

抄写输入，k=4，每条返回 128 token：

| batch | 无投机 tok/s | 投机 tok/s | 加速 |
|---:|---:|---:|---:|
| 1 | 219.9 | 847.8 | 3.86× |
| 4 | 835.6 | 3100.9 | 3.71× |
| 16 | 2808.4 | 7959.9 | 2.83× |
| 64 | 7289.8 | 11792.7 | 1.62× |

所测点上加速比下降，batch=64 仍有正收益。它支持“在这组配置中，并发增加时相对收益下降”，不支持“高并发必然失效”。还需记录草稿及验证时间，区分算力、内存和调度因素。

固定 batch=1、每次返回 200 token，另一组扫描的无投机基线为 214.8 tok/s：

| k | tok/s | 加速 |
|---:|---:|---:|
| 1 | 388.3 | 1.81× |
| 2 | 567.7 | 2.64× |
| 4 | 898.0 | 4.18× |
| 8 | 1444.4 | 6.72× |

k=8 是所测点中的最快值，不是已经找到的全局最优值。此组未记录各 k 的接受率，也没有重复测量分布。

### 消融与补测

1. 修正统计钩子的安装与清零位置，保存每轮草稿长度、接受长度、输出和停止原因。
2. 向 k=12、16 扩展扫描，比较草稿时间、验证时间及端到端吞吐，而不只比较接受率。
3. 对同一输入验证普通与投机的贪心 token 序列；随机模式另做分布检查，不能要求同 seed 的序列逐位相同。
4. 补 SGLang 和可运行的 MTP/EAGLE 路线。先核算主干、草稿、KV、激活与 workspace 的总容量；不能用“激活参数少”判断全部权重能否装入 32 GB。

## 原始现场

原始输出来自上述版本与硬件，按 A/B/C 组查数值。D/E 中关于“验证成本”“主流”“高并发必然无收益”等解释没有独立证据；保留原文便于追溯，以正文限定后的结论为准。

{{srcfold:results/crater/engine/55_speculative.txt}}

{{srcfold:labs/L5/speculative.py}}

## 前沿

后续可比较动态草稿长度、树形候选和目标/草稿共享状态。它们改变的分别是每轮预算、验证形状和重复计算，不能仅按草稿方法名称推断收益。MTP、EAGLE、Medusa 与 suffix 在本章均为 `UNVERIFIED`；需要兼容权重、固定版本源码以及同一负载下的接受长度和时间记录。

## 陷阱

1. **把聚合接受率当作各位置条件接受概率。** 几何级数模型需要额外假设。
2. **跨实验反推验证成本。** k=4 的统计不能直接解释 k=8 的耗时。
3. **随机验证只比较 token 是否相同。** 保持分布需要正确的接受与残差规则。
4. **只回退输出，不回退缓存。** 下一轮会读到不属于已提交前缀的状态。
5. **只报“快几倍”。** 同时报告模型、草稿方法、输入、并发、长度、计时与停止规则。
6. **把类型枚举当作性能证据。** 声明支持不等于当前硬件上已经运行。

## 自测题

<details markdown="1"><summary>1. k=4，所有草稿都接受时，一轮最多前进多少 token？</summary>

通常是 5 个：4 个草稿与 1 个目标模型额外 token。EOS、最大输出长度及具体接口的提交边界可能使其少于 5。
</details>

<details markdown="1"><summary>2. p=0.957、k=8 时得到 E[N]≈7.60，能否据 6.72× 算出验证只贵 13%？</summary>

不能。这要求 p 是同一实验的适当条件接受概率，且加速比来自相同稳态口径。本章的 95.7% 来自 k=4 的聚合计数；k=8 没有这些统计。总迭代成本也不等于目标验证前向成本。
</details>

<details markdown="1"><summary>3. 拒绝后为什么不能简单地从目标分布再抽一次？</summary>

接受分支已经贡献 min(P,Q) 的概率质量。拒绝分支应补回剩余的正差，而不是再按 P 增加一份，否则最终分布一般不等于 P。可用 mini 实现枚举验证。
</details>

<details markdown="1"><summary>4. batch=64 的收益低于 batch=1，是否应该关闭投机？</summary>

不一定。本组仍有 1.62× 吞吐收益。还要检查目标 SLO 下的延迟分布、草稿资源及并发容量，再决定是否动态关闭。
</details>

<details markdown="1"><summary>5. 模型自带 MTP 头，是否意味着草稿没有成本？</summary>

不是。预测模块仍需执行，也可能持有独立缓存；验证、状态传递与回退也有成本。是否更省取决于具体架构、实现和输入分布。
</details>
