---
machine: crater (RTX 5090 D, sm_120) · OLMoE-1B-7B-0924-Instruct（64 专家 / top-8）
measured: 2026-09-11
deps: 4.1（参数量的账）· 3.3（decode 的带宽账）· 2.7（瘦长 GEMM 的效率）
---

## 本章回答三个问题

MoE 为每个 token 选择部分专家参与计算，但仍需管理全部专家权重。本章分别分析激活参数量、权重容量和路由开销。

1. 总参数量与每 token 激活参数量分别决定哪些资源需求？
2. 路由是均衡的吗？不均衡对系统意味着什么？
3. 按专家分组执行 GEMM 会增加哪些调度与搬运成本？

用的是 `allenai/OLMoE-1B-7B-0924-Instruct`：16 层、64 个专家、top-8、
hidden 2048、专家 intermediate 1024。

---

## 一 · 激活参数量与权重容量分别核算

```
一个专家 = 3×2048×1024 = 6,291,456 参数
一层有 64 个专家 + 1 个 router(2048×64)

                          总参数       每 token 激活       比值
MoE 部分/层           402,784,256       50,462,720     8.0×
attention/层           16,777,216       16,777,216     1.0×
每层合计               419,561,472       67,239,936     6.2×
16 层 + embedding    6,816,006,144    1,178,861,568     5.8×
```

**6.82B 总参数，每 token 只激活 1.18B（5.8×）。**

MoE 那一层的比值恰好是 $64/8 = 8.0\times$——因为每 token 只走 8 个专家。
attention 是 1.0×（每 token 都要走全部 attention 权重），
所以整层被摊薄到 6.2×，全模型 5.8×。

但：

```
bf16 权重显存：全部 12.70 GiB，而每 token 只用到 2.20 GiB 的量
**但那 12.70 GiB 必须全部驻留显存** —— 你不知道下一个 token 会路由到谁。
```

MoE 的权重容量按总参数量计算，单个 token 的主要计算量则取决于被激活的参数。

在相同存储精度下，6.8B MoE 与 6.8B 稠密模型的权重容量接近。MoE 减少每个 token 参与计算的权重，但实际速度还取决于路由、分组执行和数据搬运。

---

## 二 · 路由有多不均衡

在 16 层的 router 上挂钩子，985 个 token 的真实文本：

```
总选择次数 126,080，理想每专家 1,970
实际 min=945  max=3511  中位数=1906
最热/最冷 = 3.7×
最热的专家拿了 2.78%（均匀应为 1.56%）
变异系数 CV = 0.295
```

跨层汇总会掩盖每层的负载差异，应进一步查看逐层分布：

| 层 | 最热占比 | 最冷占比 | 最热/最冷 | CV |
|---:|---:|---:|---:|---:|
| 0 | 9.51% | **0.00%** | 749.0× | 1.032 |
| 1 | 10.66% | 0.20% | 52.5× | 1.145 |
| 2 | 11.37% | **0.01%** | **896.0×** | 1.232 |
| 3 | 7.97% | 0.00% | 628.0× | 0.996 |
| 13 | 8.17% | 0.00% | 644.0× | 1.106 |
| 15 | 9.16% | 0.00% | 722.0× | 1.206 |

所列层存在明显不均衡，但不是每行都出现零计数。脚本计算最热/最冷时将零分母替换为 1，因此 749× 等值不是严格的最大值/最小值；应同时保留零计数和 max/mean。

::: warning 跨层求和会把不均衡抹掉
总体 CV 0.295 vs 逐层 CV 1.0–1.2，差了 3–4 倍。

原因：**不同层的热专家不是同一批**，加起来互相填平了。

而系统关心的是**每一层**——每层各做一次 grouped GEMM，
每层的最热专家各自决定那一层的关键路径。
因此，跨层汇总不能替代逐层负载分析。

这是一个很容易犯的统计错误：把需要分组看的量聚合起来看。
本书里 0.3 §陷阱 1（只报一个"吞吐"）、5.1 §三（只报吞吐掩盖 TTFT/TPOT）
都是同一个形态。
:::

用逐层的数算系统含义：

```
每层 985 个 token × top-8 = 7880 次选择，均匀应为每专家 123 个
实测最热的专家拿到该层的 11.8%，即 933 个 token —— 是均匀值的 7.6 倍
grouped GEMM 若各段并行执行，总时间由最大的那一段决定，
所以并行效率的上界就是 1/7.6 = 13%。
```

13% 只对应一个简化模型：每个专家独占等速执行单元、耗时与 token 数成正比且不能跨单元拆分。真实 grouped GEMM 可按 tile 分工，EP 中一张卡还可持有多个专家，不能直接以此作为 GPU 或 EP 效率上界。

（注意 `router_aux_loss_coef = 0.01`——训练时**有**负载均衡的辅助损失。
即使如此，推理时在具体文本上仍然这么不均。
辅助损失优化的是训练分布上的期望，不保证任意输入上的均衡。）

---

## 三 · Python 分组模拟包含多项执行成本

一步 4096 个 token、top-8，即 32768 次专家调用：

| 做法 | ms | TFLOP/s |
|---|---:|---:|
| grouped GEMM（含排序） | 1.379 | 99.7 |
| 　其中只排序 | 0.255 | — （占 18.5%） |
| 同样 FLOP 的单个大 GEMM（性能参照） | **0.627** | **219.3** |

**分成 64 个小 GEMM 比一个大 GEMM 慢 2.2×。**

原因是每个小 GEMM 的 $M$ 只有约 512，远小于 tensor core 喜欢的形状——
2.7 §六 量过瘦长 GEMM 的效率远低于方阵（那次是 cuBLAS 在 decode 形状上
只有 1465 GB/s 而方阵能到 213 TFLOP/s）。

而且**排序就占了 18.5%**——把 token 按专家重排是纯开销，不产生任何 FLOP。

::: note 2.2× 是具体模拟与单 GEMM 的时间比，不是严格上界
本章用 Python 循环逐个专家执行 GEMM，还包含排序、gather、`counts.tolist()` 的同步和结果拼接。单 GEMM 对照复用一份权重，访存条件也不同。
真实实现用**专门的 grouped GEMM kernel**：一次 launch 处理所有分段，
每个 block 根据自己的 block id 查表决定处理哪一段。

专用 kernel 可以减少逐专家提交，但不能用独立 launch 微基准乘以次数来推算端到端收益。
**本节量的是"用最朴素的方式实现会有多贵"**，
它的用处是说明为什么必须写专门的 kernel。
（同 3.3 §四 分页那次的定位。）
:::

不均衡会让情况更糟：

```
均匀              max=  575 min= 462 max/mean= 1.12×
倾斜（一个专家占 20%） max= 7061 min= 357 max/mean=13.79×
```

分段 GEMM 若并行执行，总时间由最大的那一段决定，
只有在每专家等速独占执行的简化模型中，`mean/max` 才给出负载效率；`max/mean` 是不均衡倍数——和第二节算出的 7.6× / 13% 是同一件事。

---

## 四 · 均匀路由模型下的专家覆盖率随 batch 增长

decode 时 batch=1 只碰 8 个专家。但 batch 一大，
不同 token 路由到不同专家，读取量迅速逼近全部。

期望被碰到的专家数 $= E\left[1 - (1 - K/E)^B\right]$：

| batch | 期望被碰到的专家数/层 | 读取量 MB | 占全部 |
|---:|---:|---:|---:|
| 1 | 8.0 | 1536.0 | 12.5% |
| 4 | 26.5 | 5085.0 | 41.4% |
| 16 | 56.4 | 10837.2 | **88.2%** |
| 64 | 64.0 | 12285.6 | **100.0%** |
| 256 | 64.0 | 12288.0 | 100.0% |

在各 token 独立、均匀选择 K 个不同专家的假设下，batch=64 的期望覆盖率接近 100%。真实路由偏斜、跨 token 相关性和缓存会改变实际读取量。

所以：**MoE 的稀疏性在大 batch 下从带宽角度基本消失**，
只在算力上仍然稀疏（每 token 仍只算 8 个专家）。

按结构分析，小 batch 可能只访问少数专家，减少读取权重的字节；batch 增大后，专家覆盖率提高，但每个专家也获得更多 token，矩阵形状随之改变。最终收益取决于路由、访存和执行效率，不能从覆盖公式直接判定哪类负载更快。

本章没有完整 MoE 与等总参数或等质量稠密模型的端到端对照，因此不对两者速度或质量排序。

---

## 五 · 设计取舍

### 5.1 专家层替换 MLP，保留本模型的 attention 路径

4.1 实测：稠密 Transformer 每层 **MLP 占 75%、attention 占 25%** 的参数。
MoE 只把 MLP 换成专家，attention 一个字节都不动。

这个选择很精准：
- MLP 是参数大头，稀疏化它收益最大
- attention 有 KV cache 的账（3.3），稀疏化它会破坏 KV 的规整性
- MLP 是逐 token 独立的，天然可以按 token 路由；attention 跨 token，不好切

本章按配置计算的 8.0×（MoE 层）→ 6.2×（含 attention）→ 5.8×（含 embedding）
这条衰减链，就是"只稀疏化了一部分"的直接体现。

**判据：稀疏化要挑那个「占比大 + 结构上可分」的部分。** 两个条件缺一不可。

### 5.2 负载均衡是训练与推理的目标错配

训练时用 aux loss 惩罚不均衡（本模型 `router_aux_loss_coef = 0.01`），
优化的是**训练分布上的期望均衡**。

但推理时面对的是**某一个具体的 batch**，而且 batch 越小方差越大。
本章实测：即使有 aux loss，单层最热专家仍拿到 11.8%（均匀 1.56%）。

这是一个结构性的错配：**训练优化期望，推理承受方差。**

工程上的应对都是在推理侧打补丁：
- capacity factor（超出容量的 token 直接丢弃或走 residual）——牺牲质量
- 把热专家复制到多张卡——牺牲显存
- 动态重排专家到卡的映射——牺牲复杂度

**没有一个是免费的**，因为问题的根源在目标函数里。

### 5.3 "7B 的模型，1B 的计算量"这句话的正确读法

拆成三句：

| 维度 | 是否随激活稀疏而减少 | 本章结构估算 |
|---|---|---|
| **算力** | 是 | 5.8×（每 token 只算 1.18B） |
| **显存** | **否** | 12.70 GiB 全部要驻留 |
| **带宽** | **取决于 batch** | batch=1 读 12.5%，batch≥64 读 100% |

宣传语只说了第一行。第二行决定了你能不能部署，
第三行决定了它在你的负载上到底快不快。

**设计原则：任何"稀疏"的说法，都要问清楚稀疏的是哪一种资源。**
算力、显存、带宽三者可以完全解耦——本章就是一个三者各不相同的例子。

---

## 六 · 动手 lab

```bash
# 设置 HF_HOME，准备 OLMoE-1B-7B-0924-Instruct 缓存；先核算 GPU 显存
python labs/L4/moe.py
```

原始输出：`results/crater/ckpt/44_moe.txt`。

{{srcfold:labs/L4/moe.py}}

**建议的消融**

1. **换一批文本重跑 [B]**（代码、中文、数学各一段），看热专家是不是同一批。
   如果不同领域的热专家不同，说明路由确实学到了分工；
   如果总是同一批，说明是退化的（router collapse）。
2. [B] 把 token 数从 985 加到 8000，看逐层 CV 会不会下降——
   这能区分"结构性不均衡"与"样本量小造成的方差"。**这条最重要。**
3. [C] 把 `T` 从 4096 降到 128（模拟小 batch decode），
   看 grouped GEMM 相对大 GEMM 的比值怎么变。
4. [D] 的公式假设路由是均匀随机的。用 [B] 实测的分布重算期望覆盖数，
   看会不会低于均匀假设（热专家集中 → 覆盖更慢）。

---

## 七 · 陷阱

1. **激活参数量不等于权重容量。** 全驻留部署按总权重核算；量化、卸载和跨卡分片是另外的设计。
2. **跨层聚合路由统计会抹掉不均衡**（本章总体 CV 0.295 vs 逐层 1.0–1.2）。
   必须逐层看。
3. **训练有 aux loss 不代表推理均衡**。期望均衡 ≠ 每个 batch 均衡。
4. **均匀路由模型的专家覆盖率不是实测读取量。** 大 batch 也不保证所有专家每轮都从显存读取。
5. **框架层的 python 循环量不出真实 grouped GEMM 的代价**（本章 2.2× 只描述具体模拟）。
6. **排序是纯开销**，本章占 grouped GEMM 的 18.5%，不产生任何 FLOP。
7. **`max/mean` 是不均衡倍数，不是效率。** EP 效率需按每卡实际负载、通信和调度分析。

---

## 八 · 自测题

<details markdown="1">
<summary>1. 一个 7B 的 MoE 和一个 7B 的稠密模型，部署时有什么区别？</summary>

**显存一样，算力不同。**

MoE 的全部专家都要驻留显存（本章实测 6.82B → 12.70 GiB bf16），
因为你不知道下一个 token 路由到谁。所以显存按**总参数量**买。

算力上 MoE 每 token 只算 1.18B（5.8×），所以同样的算力预算能跑更大的"有效模型"。

另外 MoE 多了两项稠密模型没有的开销：
- 路由 + 按专家排序（本章占 grouped GEMM 的 18.5%）
- 负载不均可能导致等待；13% 仅是等速、每专家独占执行模型的估计，不是 EP 实测

**所以 MoE 的正确定位是：显存换质量，不是省资源。**
</details>

<details markdown="1">
<summary>2. 你测出 MoE 的路由「总体最热/最冷只有 3.7 倍」，能说明负载还算均衡吗？</summary>

不能——**这是聚合错误**。

本章实测：总体 3.7×、CV 0.295，看着不错；但逐层最热/最冷高达 896×，
CV 1.0–1.2，而且**每层都有专家拿到 0.00%**。

原因是不同层的热专家不是同一批，求和时互相填平了。

而系统关心的是每一层：每层各做一次 grouped GEMM，
每层的最热专家各自决定那一层的关键路径。

正确的看法是**逐层统计**，而且指标要用 `max/mean`（不均衡倍数），
不是 `max/min`（受最冷那个的噪声影响）也不是 CV（对长尾不敏感）。
</details>

<details markdown="1">
<summary>3. MoE 在小 batch decode 上能减少多少专家读取？</summary>

单个 token 选择 8/64 个专家时，选中专家的权重容量为全部专家的 12.5%。若 batch 内各 token 独立均匀选 K 个专家，期望覆盖数为 $E[1-(1-K/E)^B]$。

这里 E 是专家数，不是期望算子；实际路由有偏斜和相关性，因此公式只作预测。减少的字节是否转化为更低延迟，还需真实 kernel、缓存和端到端对照，不能直接认定小 batch 收益有限。

</details>

<details markdown="1">
<summary>4. 本章 MoE 为什么将 MLP 替换为专家？</summary>

MLP 在所测模型中占主要参数，且逐 token 独立，适合按 token 路由。将其替换为专家后，attention 路径可以保持原有结构。

这不表示 attention 无法稀疏化，也不能推广为所有 MoE 只能放在 MLP 中。不同稀疏方案需要定义状态表示、路由语义和质量目标。

</details>
