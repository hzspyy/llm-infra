---
machine: crater · RTX 5090 D · vLLM 0.29.0 · SGLang 0.5.19
measured: 2026-09-09
deps: 可先读 L0.0；分词与采样的细节见 L0.4、L0.5
---

## 一次请求经过的组件

你向模型发送一条消息，最后收到一段逐字出现的回答。两端都像是在处理文字，
但 GPU 并不直接读取聊天消息。服务器需要先把文字变成模型的输入，
安排一次或多次计算，再把计算结果变回文字。

本章沿请求处理过程介绍推理引擎，先区分三个组件：

- API 服务接收请求、检查参数，并把输出发回客户端。
- 调度器管理等待中和执行中的请求，决定下一轮处理谁。
- 模型执行器准备张量、调用模型，并把工作提交给 GPU。

这三个角色不一定各占一个进程。同一个进程可以承担多个角色，
一个角色也可以由多个 worker 承担。读源码时，先分清职责，再看进程划分，
比先记住“某引擎有几个进程”更有用。

本章的主线是普通文本生成。图片、投机解码和多机部署会增加步骤，
不能把这里的流程当成所有配置的完整调用栈。

## 从聊天消息变成模型输入

假设请求中的消息是“请用一句话解释显存带宽”。API 首先解析 HTTP 请求中的 JSON，
确认消息格式、模型名称和生成参数是否合法。这时处理的仍是普通应用数据。

接下来，服务将消息交给 chat template。模板会加入角色标记、消息分隔符，
也可能加入模型约定的生成提示。相同的问题套用不同模板后，得到的字符串可能不同。
模板内容会直接影响模型输入。

tokenizer 再把字符串分成 token，并为每个 token 找到词表中的整数编号。
模型接收的是这些编号，不是 Python 字符串。这里要分别检查三份东西：
原始 messages、模板展开后的文本、最终 token id。只检查最初的消息，
很容易漏掉模板或特殊 token 的差异。

下方原始材料展示了真实的词表项与模板。展开后，可先对比普通词条与特殊 token，再查看模板结构：

{{srcfold:results/crater/raw/13_tokenizer.txt}}

词条中的 `Ġ` 常表示前导空格。“带空格的词片”和“不带空格的词片”可能是不同 token。
但末尾多一个空格不一定使整个前缀失效；应直接比较实际 token 序列。

## 调度器组织并发请求

服务器可能同时收到多个请求。它们的输入长度不同，已经生成的长度也不同，
有些刚到达，有些只差一步就会结束。调度器的任务是把这些状态组织成下一轮计算。

它需要同时考虑两类限制：本轮可以处理多少 token，以及显存是否足以保存所需状态。
因此，“请求已经进入服务器”不等于“GPU 已经开始计算它”。排队时间属于请求延迟，
但不属于某个模型算子的执行时间。

对于使用前缀缓存的请求，还需要查询已有缓存。KV cache 保存历史位置的
key 和 value 张量；如果缓存与当前输入的前缀兼容，就可能少算一部分输入。
分页管理与前缀查找是两件事：前者决定 KV 存放位置，后者决定已有 KV 能否复用。
L5.2 会单独展开这两个问题。

vLLM 的块哈希把前面的上下文带入身份计算；SGLang 的基数树显式保存前缀分叉。
这两种表示方式各有取舍，但不能从数据结构名称直接推出哪一个更快，
也不能说块哈希原则上无法提供前缀感知调度需要的信息。

## 执行器将调度结果整理成张量

调度器的决定通常还是 CPU 上的数据结构。执行器需要把它整理成 GPU 能读取的张量。
其中几项在后面的章节会反复出现：

| 输入 | 含义 | 为什么需要 |
|---|---|---|
| token id | 本轮各位置对应的词表编号 | 查 embedding，开始模型计算 |
| position | 当前 token 在序列中的位置序号 | 计算 RoPE 等位置编码变换 |
| slot mapping | 新 K/V 应写入的槽位 | 把新状态放入正确的缓存位置 |
| block table | 逻辑块对应的物理块编号 | 寻址历史 K/V |
| attention metadata | 长度、偏移等辅助信息 | 让 attention kernel 正确处理变长序列 |

在小模型或短序列场景下，CPU 构造并向 GPU 传输这些元数据张量可能占据可观的时间。

GPU 随后执行模型。一次前向通常包含矩阵乘、归一化、位置变换、
attention 和激活函数等多个操作。框架可能把其中一些操作融合，
也可能为一个操作调用多个 kernel。因此，模型层数、算子数和 kernel 数不能混用。

## 首个输出来自 prefill，后续输出来自 decode

在普通自回归生成中，第一次前向处理 prompt，通常称为 prefill。
末位置的隐藏状态经过 lm_head 得到 logits，采样器据此选择第一个输出 token。
**第一个输出来自这次 prompt 前向，不需要先额外执行一次 decode 前向。**

为了生成第二个输出，模型再处理上一步选出的 token，并读取历史 KV。
这时新增输入通常只有一个 token，而历史上下文已经较长。这类后续步骤称为 decode。
如果一个 batch 中有多条请求，每条请求都可能贡献自己的新 token。

同一模型在两个阶段会出现不同形状的矩阵计算：prefill 的线性层一次处理较多位置，
小 batch decode 一次处理的位置较少。L0.2 将据此估算计算量和数据量，
L0.3 再比较硬件上的表现。

拿到 token id 后，服务还要执行增量解码，把编号转换成可发送的文本，
检查停止条件，并通过流式响应返回。一次网络事件未必恰好对应一个 token：
缓冲策略、解码器和接口协议都可能影响发送时机。

## 工程实现 · 源码入口与进程边界

下面梳理了各主要模块的关键代码入口与控制流向（静态架构映射）：

{{srcfold:results/crater/stack_map.md}}

初次阅读可以先追踪一段核心链路：从请求进入调度器，到执行器准备模型输入。
阅读每个函数时，注意观察它接收什么对象、修改哪个状态、结束后把控制权交给谁。
遇到跨进程通信，应顺藤摸瓜找到发送端与接收端的对应逻辑。

随着上游代码迭代，文件结构可能会有所调整。下方辅助脚本记录了各阶段的核心模块与符号，可在运行时动态定位行号：

{{srcfold:labs/L0/map_stack.py}}

多进程架构还会影响性能观测：profiler 挂在哪个进程，决定了它能捕获到哪些事件。
若 GPU 计算发生在工作子进程，父进程的 profiler 将无法捕获相应的 kernel 执行。
为便于本地观察，本章实验采用单进程配置；在评估生产多进程部署时需考虑跨进程通信开销。

## 读一份真实 kernel 记录

下方的 kernel 运行记录采集自 Qwen3-1.7B（eager 模式，prefill 长度 1024，batch 为 1），列出了各算子 kernel 名称、调用次数与累计耗时。分析时建议优先关注矩阵乘（GEMM）与 attention，再看 KV cache 写入与采样环节：

{{srcfold:results/crater/kernel_trace.json}}

同一个符号可能执行多次，通常与层数、生成步数或分块有关。
分析调用频次时需先确认统计区间：是一次前向、多步生成的总和，还是通过差分估算的单步开销。不能把累计次数直接等同于“每步次数”。

此外，还需区分 GPU 执行时间与端到端墙钟时间。两者的差值包含了排队、CPU 调度、同步及工具开销。由于 CPU 提交与 GPU 执行存在流水线重叠，不能直接用“kernel 次数 × 单次 launch 开销”去粗暴分解请求总耗时。

配套的测量脚本同时提供了 profiler 追踪与原生计时两条路径：

{{srcfold:labs/L0/kernel_trace.py}}

## 模型文件和机器码，分别长什么样

先看模型配置。例如 `hidden_size` 决定隐藏向量的维度，
`num_hidden_layers` 决定层数，`num_key_value_heads` 参与决定 KV 的大小。
配置告诉你模型打算采用什么结构，仍需结合加载代码检查实际实现。

{{srcfold:results/crater/raw/11_config.json}}

再看权重清单。每项都有名称、形状和 dtype。名称连接模型代码与存储的张量；
形状可用于核对矩阵乘法是否匹配。磁盘里的张量元素总数不一定等于独立参数量，
例如共享权重可能有不同存储表示。L4.0 会继续检查加载后的共享关系。

{{srcfold:results/crater/raw/12_weight_manifest.txt}}

safetensors 的字节级记录可以帮助你把文件中的张量与内存中的数值联系起来。
先看头部长度，再看某个张量的偏移，最后看该位置的原始字节如何解码：

{{srcfold:results/crater/raw/40_safetensors_anatomy.txt}}

模型调用的 kernel 也有编译产物。PTX 是中间表示，SASS 是具体 GPU 的机器指令。
下面两段文件截取了部分编译产物（PTX 中间代码与 SASS 机器指令），直观展示底层的机器形态；详细的指令集与寄存器布局分析留待 L2.2 展开。

{{srcfold:results/crater/raw/32_ptx_head.txt}}

{{srcfold:results/crater/raw/34_sass_head.txt}}

## 怎样自己复现这条阅读路径

实践时可先结合源码与原始工件，梳理出请求对象、token 张量、缓存元数据与输出对象的流转关系：由哪个组件创建、在何时传递给谁。随后可尝试在脚本中修改关注的目标符号，练习源码定位。

运行前，请准备好模型和所需依赖，并确认 GPU 资源可用。先用脚本的 `--help` 检查参数，再按已确认的环境运行。依赖和工具链应安装在独立虚拟环境中。

下方的环境记录汇总了测试时的软硬件依赖版本，供复现时对照环境基线：

{{srcfold:results/crater/raw/22_toolchain.txt}}

## 自测：能否把几个边界分开

<details markdown="1">
<summary>模型输出了 token id，为什么客户端还没有收到文字？</summary>

模型计算结束并不等于响应发送完成。服务端仍需将结果拷贝回主机、
执行增量解码（detokenize）、判定停止条件，最后写回网络响应通道。排查延迟时应当将这些步骤逐一拆解，
不能把所有耗时都归咎于模型前向计算。
</details>

<details markdown="1">
<summary>一份 trace 显示 400 次 kernel 调用，能否说模型有 400 层？</summary>

不能。一层可能调用多个 kernel，同一个 kernel 也可能被多层复用。
先确认统计区间，再根据符号、次数和对应代码判断这些调用属于哪些操作。
</details>

<details markdown="1">
<summary>使用 CUDA Graph 后只提交一次，GPU 是否就只执行一个 kernel？</summary>

不是。一张图可以包含多个 kernel 节点。图重放减少重复提交工作，
算子融合才可能减少独立 kernel 的数量。两种方法可以组合，但应分别观察。
</details>

## 接下来读什么

L0.2 计算权重、KV 与运算量；L0.3 用同一组工作量比较两种 GPU。
L5.2 继续讨论缓存身份、块引用和淘汰，L5.4 再展开图执行与重叠。
这些章节的共同问题是：一次请求需要做哪些工作，哪些工作可以省略、复用或重叠，
以及这样做需要满足什么条件。
